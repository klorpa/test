;=================================================================
;戦闘難易度関連設定スイッチに関する関数郡
; FLAG:戦闘難易度関連設定スイッチ にBIT値で設定を記録する
;  0 = 素質関連設定
;  1 = バステ命中設定
;  2 = 1more関連設定
;  3 = 装備耐性・効果変更設定
;=================================================================

;-----------------------------------------------------------------
;@BATTLE_SETTING_IS_TALENT
;戦闘時にキャラの素質がスキル等に影響を与えるかの設定を返す
;　引数　なし
; 戻り値 0=設定が無効 1=設定が有効
;-----------------------------------------------------------------
@BATTLE_SETTING_IS_TALENT
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;設定が有効
	SIF GETBIT(FLAG:戦闘難易度関連設定スイッチ, 0) == 1
		RETURNF 1
	;設定が無効
	RETURNF 0

;-----------------------------------------------------------------
;@BATTLE_SETTING_INVERT_TALENT
;戦闘時にキャラの素質がスキル等に影響を与えるかの設定を行う、関数を呼び出すごとにON/OFF変更
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@BATTLE_SETTING_INVERT_TALENT
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:戦闘難易度関連設定スイッチ, 0

;-----------------------------------------------------------------
;@BATTLE_SETTING_SET_TALENT
;戦闘時にキャラの素質がスキル等に影響を与えるかの設定を行う
;　引数　ARG:0 0=設定が無効 1=設定が有効
; 戻り値 なし
;-----------------------------------------------------------------
@BATTLE_SETTING_SET_TALENT, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:戦闘難易度関連設定スイッチ, 0
	ELSE
		SETBIT FLAG:戦闘難易度関連設定スイッチ, 0
	ENDIF

;-----------------------------------------------------------------
;@BATTLE_SETTING_IS_BAD_STATUS
;戦闘時に特定のバステで必ず命中するかの設定を返す
;　引数　なし
; 戻り値 0=設定が無効 1=設定が有効
;-----------------------------------------------------------------
@BATTLE_SETTING_IS_BAD_STATUS
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;設定が有効
	SIF GETBIT(FLAG:戦闘難易度関連設定スイッチ, 1) == 1
		RETURNF 1
	;設定が無効
	RETURNF 0

;-----------------------------------------------------------------
;@BATTLE_SETTING_INVERT_BAD_STATUS
;戦闘時に特定のバステで必ず命中するかの設定を行う、関数を呼び出すごとにON/OFF変更
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@BATTLE_SETTING_INVERT_BAD_STATUS
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:戦闘難易度関連設定スイッチ, 1

;-----------------------------------------------------------------
;@BATTLE_SETTING_SET_BAD_STATUS
;戦闘時に特定のバステで必ず命中するかの設定を行う
;　引数　ARG:0 0=設定が無効 1=設定が有効
; 戻り値 なし
;-----------------------------------------------------------------
@BATTLE_SETTING_SET_BAD_STATUS, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:戦闘難易度関連設定スイッチ, 1
	ELSE
		SETBIT FLAG:戦闘難易度関連設定スイッチ, 1
	ENDIF

;-----------------------------------------------------------------
;@BATTLE_SETTING_IS_1MORE
;戦闘時に1more動作を変更するかの設定を返す
;　引数　なし
; 戻り値 0=設定が無効 1=設定が有効
;-----------------------------------------------------------------
@BATTLE_SETTING_IS_1MORE
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;設定が有効
	SIF GETBIT(FLAG:戦闘難易度関連設定スイッチ, 2) == 1
		RETURNF 1
	;設定が無効
	RETURNF 0

;-----------------------------------------------------------------
;@BATTLE_SETTING_INVERT_1MORE
;戦闘時に1more動作を変更するかの設定を行う、関数を呼び出すごとにON/OFF変更
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@BATTLE_SETTING_INVERT_1MORE
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:戦闘難易度関連設定スイッチ, 2

;-----------------------------------------------------------------
;@BATTLE_SETTING_SET_1MORE
;戦闘時に1more動作を変更するかの設定を行う
;　引数　ARG:0 0=設定が無効 1=設定が有効
; 戻り値 なし
;-----------------------------------------------------------------
@BATTLE_SETTING_SET_1MORE, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:戦闘難易度関連設定スイッチ, 2
	ELSE
		SETBIT FLAG:戦闘難易度関連設定スイッチ, 2
	ENDIF

;-----------------------------------------------------------------
;@BATTLE_SETTING_IS_EQUIP_EFFECT
;装備耐性・効果を変更するかの設定を返す
;　引数　なし
; 戻り値 0=設定が無効 1=設定が有効
;-----------------------------------------------------------------
@BATTLE_SETTING_IS_EQUIP_EFFECT
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;設定が有効
	SIF GETBIT(FLAG:戦闘難易度関連設定スイッチ, 3) == 1
		RETURNF 1
	;設定が無効
	RETURNF 0

;-----------------------------------------------------------------
;@BATTLE_SETTING_INVERT_EQUIP_EFFECT
;装備耐性・効果を変更するかの設定を行う、関数を呼び出すごとにON/OFF変更
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@BATTLE_SETTING_INVERT_EQUIP_EFFECT
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:戦闘難易度関連設定スイッチ, 3

;-----------------------------------------------------------------
;@BATTLE_SETTING_SET_EQUIP_EFFECT
;装備耐性・効果を変更するかの設定を行う
;　引数　ARG:0 0=設定が無効 1=設定が有効
; 戻り値 なし
;-----------------------------------------------------------------
@BATTLE_SETTING_SET_EQUIP_EFFECT, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:戦闘難易度関連設定スイッチ, 3
	ELSE
		SETBIT FLAG:戦闘難易度関連設定スイッチ, 3
	ENDIF

