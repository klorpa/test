;=============================================================================
;日記帳.exe_rev0205
;=============================================================================
;--------------------------------------------------
;関数呼び出し
;--------------------------------------------------
@SHOPCOMABLE_888
RESULTS = Diary.exe
RETURN 1

@SHOP_COM_888
			CALL DIARY_DUNGEON_MAIN
RETURN 1

;--------------------------------------------------
;メインページ
;--------------------------------------------------
@DIARY_DUNGEON_MAIN
P = 0
$PRINT_LIST
DRAWLINE
PRINTL 【Diary.exe ver0.205】
PRINTL This is a record of the different worlds or dungeons you know about.
PRINTL Places that have not yet been cleared are displayed in white.
PRINTL Already cleared places and places you can no longer return to are displayed in light gray.
PRINTL Unknown places are displayed as ??? until you discover them.
PRINTL (However, even in the japanese version, there isn't really any functionality to this aside from displaying the list)
DRAWLINE

;コマンドの表示
;進捗管理も兼ねてこういう書き方です

CALL DIARY_DUNGEON_0
CALL DIARY_DUNGEON_1
CALL DIARY_DUNGEON_2
CALL DIARY_DUNGEON_3
;私家版対応済み
CALL DIARY_DUNGEON_4
;CALL DIARY_DUNGEON_5
;大魔宮は関数作ったけどクリア条件の絞り込みが出来てないんで読み込まない
CALL DIARY_DUNGEON_6
;コロシアムは私家版だとフラグが違う
CALL DIARY_DUNGEON_7
CALL DIARY_DUNGEON_8
;私家版追加ダンジョン
CALL DIARY_DUNGEON_9
CALL DIARY_DUNGEON_10
CALL DIARY_DUNGEON_11
CALL DIARY_DUNGEON_12
;13-15は欠番
CALL DIARY_DUNGEON_16
CALL DIARY_DUNGEON_17
CALL DIARY_DUNGEON_18
CALL DIARY_DUNGEON_19
;CALL DIARY_DUNGEON_20
;ギンザ大地下道も関数作ったけどクリア条件の絞り込みが出来てないんで読み込まない
;21-23は欠番
CALL DIARY_DUNGEON_24
CALL DIARY_DUNGEON_25
CALL DIARY_DUNGEON_26
;27は欠番
CALL DIARY_DUNGEON_28
CALL DIARY_DUNGEON_29
;赤坂は実装済み　ただしヒルコとのコンタクトをクリア条件にしているのでやや不安
CALL DIARY_DUNGEON_30
;私家版追加ダンジョン
;CALL DIARY_DUNGEON_31
;私家版追加ダンジョンだが、デパート系ダンジョンなので
;クリアの判断基準が困難　実装先送り
;32は欠番
CALL DIARY_DUNGEON_33
;ゾウシガヤ霊園についてはクリアに"ならない"状態　フラグの集積がめんどい
;ダンジョンフラグ33:1-12で個別に霊園ボス撃破を管理しており、
;1_12を全部掛け算して0にならない、みたいな式がさくっと書ければ実装できる
CALL DIARY_DUNGEON_34
;私家版追加ダンジョン
CALL DIARY_DUNGEON_35
;私家版追加ダンジョン
CALL DIARY_DUNGEON_36
;私家版追加ダンジョン
CALL DIARY_DUNGEON_37
;私家版追加ダンジョン
CALL DIARY_DUNGEON_38
;私家版追加ダンジョン
CALL DIARY_DUNGEON_39
;私家版追加ダンジョン
CALL DIARY_DUNGEON_40
CALL DIARY_DUNGEON_41
CALL DIARY_DUNGEON_42
CALL DIARY_DUNGEON_43
CALL DIARY_DUNGEON_44
CALL DIARY_DUNGEON_45
CALL DIARY_DUNGEON_46
CALL DIARY_DUNGEON_47
CALL DIARY_DUNGEON_48
CALL DIARY_DUNGEON_49
CALL DIARY_DUNGEON_50
;STRFLAG関数をIF内で使っても値が変わっちゃうみたいなんですけお！！
;誰か使い方教えてください　今は判定後無理やり0に戻してるクソ対応です
CALL DIARY_DUNGEON_51
;あれ、STRFALGの管理ってこれでいいのか？
;NUM_EVのやりかたしかわからない _Dはうまくいかなかった
CALL DIARY_DUNGEON_52
CALL DIARY_DUNGEON_53
CALL DIARY_DUNGEON_54
CALL DIARY_DUNGEON_55
;56-58は欠番
CALL DIARY_DUNGEON_59
;私家版追加ダンジョン
CALL DIARY_DUNGEON_60
CALL DIARY_DUNGEON_61
;私家版だと原宿の管理フラグが違う
;具体的には出現条件なし、イベント進行度フラグの管理が61:0 == 5でクリア
CALL DIARY_DUNGEON_62
;私家版追加ダンジョン
CALL DIARY_DUNGEON_63
;私家版追加ダンジョン
CALL DIARY_DUNGEON_64
;私家版追加ダンジョン
CALL DIARY_DUNGEON_65
;シュバルツバース進行度で管理されているが、>= 4で止まっていておそらく未完成
;現状は実装分の最後まで進んでいればクリアとする
CALL DIARY_DUNGEON_66
;67-69は欠番
CALL DIARY_DUNGEON_70
;Tartarusはニュクスアバター撃破をクリア条件として使用
;71は欠番
CALL DIARY_DUNGEON_72
CALL DIARY_DUNGEON_73
CALL DIARY_DUNGEON_74
;私家版追加ダンジョン
CALL DIARY_DUNGEON_75
;76-77は欠番
CALL DIARY_DUNGEON_78
CALL DIARY_DUNGEON_79
CALL DIARY_DUNGEON_80
;私家版追加ダンジョン
CALL DIARY_DUNGEON_81
;私家版追加ダンジョン
CALL DIARY_DUNGEON_82
CALL DIARY_DUNGEON_83
CALL DIARY_DUNGEON_84
CALL DIARY_DUNGEON_85
;私家版追加ダンジョン
CALL DIARY_DUNGEON_86
;私家版追加ダンジョン
;87は欠番
CALL DIARY_DUNGEON_88
;私家版追加ダンジョン
CALL DIARY_DUNGEON_89
CALL DIARY_DUNGEON_90
;91は欠番
CALL DIARY_DUNGEON_92
CALL DIARY_DUNGEON_93
;CALL DIARY_DUNGEON_94
;私家版追加ダンジョン
;クリアが実質存在しないため実装見送り
CALL DIARY_DUNGEON_95
;私家版追加ダンジョン

;--------------------------------------------------
;200 戻る処理
;--------------------------------------------------

PRINTL 
DRAWLINE
PRINTLC [888]Return

;--------------------------------------------------
;メインのインプットループ
;--------------------------------------------------

$INPUT_LOOP
INPUT
IF RESULT == 888
	RETURN 0
;コマンド領域を設定
;コマンドは飾りにしてみる
;ELSEIF RESULT < 0 || RESULT > 1
ELSE
	GOTO INPUT_LOOP
;本来のINPUTは使わない
;ELSE
;	TRYCALLFORM DIARY_DUNGEON_{RESULT}
;	GOTO PRINT_LIST
ENDIF

;--------------------------------------------------
;ダンジョンごとのDIARY関数の記述
;--------------------------------------------------

;--------------------------------------------------
;0,サンプルダンジョン,DEBUG
;--------------------------------------------------
@DIARY_DUNGEON_0
;出現フラグのチェック　デバッグモードでしか出ません
IF FLAG:DEBUG == 1
	;クリアフラグのチェック
	IF FLAG:DEBUG == 1
		SETCOLOR COLOR("light-gray")
		PRINTLC [ 0] Sample dungeon
		RESETCOLOR
	ENDIF
	;サンプルダンジョンについて出現してない場合は[?]を表示しない
ENDIF

;--------------------------------------------------
;1,エコービル,,,無条件,,,,ダンジョンフラグ:1:1 = 1,,,
;--------------------------------------------------
@DIARY_DUNGEON_1
;出現フラグのチェック　エコービルは無条件
;IF FLAG:DEBUG == 1
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_1
	IF ダンジョンフラグ:1:1 == 1
		;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 1] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
		;出現しているが未クリア
		PRINTFORMLC [ 1] %RESULTS, 20, LEFT%
	ENDIF
;ENDIF

;--------------------------------------------------
;2,軽子坂高校,,,依頼フラグ:2:0 == 1,依頼フラグ:3:0 == 1,ダンジョンフラグ:7:1,,ダンジョンフラグ:2:11 = 1,,,
;--------------------------------------------------
@DIARY_DUNGEON_2
;出現フラグのチェック　出現フラグを満たしているorクリアフラグを立てている
IF 依頼フラグ:2:0 == 1 && 依頼フラグ:3:0 == 1 || ダンジョンフラグ:2:12 == 1
	;出現している
	;クリアフラグのチェック
	;ダンジョン7をクリアしていない　を満たしてもクリア扱いに
	CALLFORM GET_DUNGEON_NAME_2
	IF ダンジョンフラグ:2:12 == 1 || ダンジョンフラグ:7:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 2] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [ 2] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ 2] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;3,新宿衛生病院
;--------------------------------------------------
@DIARY_DUNGEON_3
;出現フラグのチェック　エコービルクリアが条件
IF ダンジョンフラグ:1:1 != 0
	;出現している
	;クリアフラグのチェック
	;私家版だとユキジョロウ陥落ルート(3:1 == 2)が追加
	CALLFORM GET_DUNGEON_NAME_3
	IF ダンジョンフラグ:3:1 >= 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 3] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [ 3] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ 3] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;4,精神世界,フラグがそれぞれ２通り
;精神世界
;--------------------------------------------------
@DIARY_DUNGEON_4
;出現フラグのチェック　百合の花ルートか直哉ルートのどっちかが5以上
IF FLAG:百合の花ルート >= 5 || FLAG:直哉ルート >= 5
	;出現している
	;クリアフラグのチェック　ルートのどっちかが6以上
	CALLFORM GET_DUNGEON_NAME_4
	IF  FLAG:百合の花ルート >= 6 || FLAG:直哉ルート >= 6
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 4] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [ 4] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ 4] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;5,大魔宮,備考,出現フラグ,クリアフラグ
;サンプルスクリプト
;--------------------------------------------------
@DIARY_DUNGEON_5
;出現フラグのチェック　大魔宮への誘いクリア
IF イベントフラグ:3:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_5
	IF ダンジョンフラグ:3:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 5] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [ 5] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ 5] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;6,コロシアムダンジョン,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_6
;出現フラグのチェック
;私家版対応済み
IF 闘技場フラグ:19:0 != 0
	;出現している
	;クリアフラグのチェック　ダンジョン・マッチに勝利済み
	CALLFORM GET_DUNGEON_NAME_6
	IF ダンジョンフラグ:6:0 == 2
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 6] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [ 6] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ 6] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;7,軽子坂上層,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_7
;出現フラグのチェック　クリアすると消えるタイプのダンジョン
IF イベントフラグ:2:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_7
	IF ダンジョンフラグ:7:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 7] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [ 7] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ 7] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;8,警視庁
;--------------------------------------------------
@DIARY_DUNGEON_8
;出現フラグのチェック　レベル30以上でした
IF BASE:MASTER:LV > 30
	;出現している
	;クリアフラグのチェック　クリアもしくはレベル上限突破
	CALLFORM GET_DUNGEON_NAME_8
	IF ダンジョンフラグ:8:12 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 8] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [ 8] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ 8] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;9,御影町地下鉄駅,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_9
;出現フラグのチェック　エコービルクリア
IF ダンジョンフラグ:1:1 == 1 || CFLAG:MASTER:72 == 21
	;出現している
	;クリアフラグのチェック
	;P1ルートクリア,プラズマウォール消失.このダンジョンクリア、のいずれかでクリアと見なす
	CALLFORM GET_DUNGEON_NAME_9
	IF ダンジョンフラグ:11:7 == 1 || イベントフラグ:24:0 != 0 || ダンジョンフラグ:9:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [ 9] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [ 9] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ 9] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;10,SEBECビル,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_10
;出現フラグのチェック　クリアすると消えるタイプ
IF ダンジョンフラグ:9:1 == 1
	;出現している
	;クリアフラグのチェック　クリアフラグが消滅フラグを兼ねている
	CALLFORM GET_DUNGEON_NAME_10
	IF ダンジョンフラグ:10:9 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [10] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [10] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [10] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;11,デヴァ・ユガ,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_11
;出現フラグのチェック　クリアしたら消えるタイプ
IF ダンジョンフラグ:10:9 == 1
	;出現している
	;クリアフラグのチェック　分岐型なので0以外
	CALLFORM GET_DUNGEON_NAME_11
	IF ダンジョンフラグ:11:7 != 0
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [11] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [11] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [11] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;12,迷いの森,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_12
;出現フラグのチェック　クリアすると消えるタイプ
IF (ダンジョンフラグ:11:7 == 2 && GETCHARA([[キャラ:麻希]], 0) != -1) || ダンジョンフラグ:12:0 == 10
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_12
	IF ダンジョンフラグ:12:0 == 10
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [12] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [12] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [12] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;16,渋谷センター街,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_16
;出現フラグのチェック　クリアすると消えるタイプ
IF 闘技場フラグ:13:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_16
	IF ダンジョンフラグ:16:14 != 0
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [16] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [16] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [16] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;17,池袋,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_17
;出現フラグのチェック　渋谷クリアが条件
IF ダンジョンフラグ:16:14 != 0
	;出現している
	;クリアフラグのチェック　水晶のピラー入手をとりあえずクリアに
	CALLFORM GET_DUNGEON_NAME_17
	IF ダンジョンフラグ:17:13 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [17] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [17] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [17] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;18,上野,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_18
;出現フラグのチェック　クリアすると消えるタイプ
IF ダンジョンフラグ:17:13 == 1
	;出現している
	;クリアフラグのチェック　木星のピラー入手をとりあえずクリアに
	CALLFORM GET_DUNGEON_NAME_18
	IF ダンジョンフラグ:18:22 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [18] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [18] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [18] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;19,アラヤの岩戸,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_19
;出現フラグのチェック　クリアすると消えるタイプ
;キャラ存在判定があるので、クリア済み条件を出現条件としてもＯＲで使う
IF (ダンジョンフラグ:12:1 == 1 && GETCHARA([[キャラ:麻希]]) >= 0) || ダンジョンフラグ:19:1 != 0
	;出現している
	;クリアフラグのチェック　結末に関係なく、何らかのクリアフラグがあればクリア
	CALLFORM GET_DUNGEON_NAME_19
	IF ダンジョンフラグ:19:1 != 0
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [19] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [19] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [19] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;20,ギンザ大地下道,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_20
;出現フラグのチェック　ユズ撃破
IF 闘技場フラグ:5:0 == 1
	;出現している
	;クリアフラグのチェック　ギンザのクリアフラグも判別が難しい
	CALLFORM GET_DUNGEON_NAME_20
	;IF ダンジョンフラグ:3:1 == 1
	;クリア済み
	;	SETCOLOR COLOR("light-gray")
	;	PRINTLC [20] ギンザ大地下道
	;	RESETCOLOR
	;ELSE
	;出現しているが未クリア
		PRINTFORMLC [20] %RESULTS, 20, LEFT%
	;ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [20] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;24,図書館,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_24
;出現フラグのチェック　エコービルクリアor初台シェルタークリアor原宿クリア クリアすると消えるタイプ
IF ダンジョンフラグ:1:1 == 1 || ダンジョンフラグ:34:8 == 1 || ダンジョンフラグ:61:0 == 5
	;出現している
	;クリアフラグのチェック　フグルマ撃破
	CALLFORM GET_DUNGEON_NAME_24
	IF ダンジョンフラグ:24:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [24] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [24] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [24] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;25,旧皇居,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_25
;出現フラグのチェック　闘技場1クリア
IF 闘技場フラグ:1:0 != 0
	;出現している
	;クリアフラグのチェック　イザナミ撃破
	CALLFORM GET_DUNGEON_NAME_25
	IF ダンジョンフラグ:25:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [25] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [25] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [25] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;26,都庁,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_26
;出現フラグのチェック　衛星病院クリア
IF ダンジョンフラグ:3:1 != 0
	;出現している
	;クリアフラグのチェック　かなり複雑なので後回し
	CALLFORM GET_DUNGEON_NAME_26
	IF ダンジョンフラグ:26:2 >= 4
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [26] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [26] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [26] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;28,ファクトリー,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_28
;出現フラグのチェック　情報の有無
IF ダンジョンフラグ:[[ダンジョン:ファクトリー]]:0 == 1
	;出現している
	;クリアフラグのチェック
	;菊枝救出済みならクリアと見なす
	;依頼の失敗もしくは拒否もクリアと見なす
	CALLFORM GET_DUNGEON_NAME_28
	IF ダンジョンフラグ:[[ダンジョン:ファクトリー]]:5 == -1 || ダンジョンフラグ:[[ダンジョン:ファクトリー]]:0 == -1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [28] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [28] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [28] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;29,赤坂,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_29
;出現フラグのチェック　情報の有無
IF ダンジョンフラグ:[[ダンジョン:赤坂]]:0 == 1
	;出現している
	;クリアフラグのチェック
	;ヒルコに何らかのコンタクトが取れていればクリアと見なす
	;ドラウプニル関連が私家版で実装されているが、ここでは判定対象にしない
	CALLFORM GET_DUNGEON_NAME_29
	IF ダンジョンフラグ:[[ダンジョン:赤坂]]:1 != 0
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [29] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [29] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [29] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;30,異端収容所,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_30
;出現フラグのチェック　クリアすると消える　フラグが数値的に管理されている
IF 依頼フラグ:70:0 >= 6
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_30
	IF 依頼フラグ:70:0 >= 7
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [30] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [30] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [30] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;31,六本木大歓楽街
;--------------------------------------------------
@DIARY_DUNGEON_31
;出現フラグのチェック　時間条件しかないので実質なし
;IF 依頼フラグ:70:0 >= 6
	;出現している
	;クリアフラグのチェック
	;一応突入済みならクリアとする
	;ただ、こういうデパート系のダンジョンは扱いが難しいので実装対象外とする
	;関数を呼び出さない形で対応
	CALLFORM GET_DUNGEON_NAME_31
	IF イベントフラグ:70:0 > 5 && ダンジョンフラグ:31:0 == 3
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [31] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [31] %RESULTS, 20, LEFT%
	ENDIF
;ELSE
;出現していない
;	SETCOLOR COLOR("gray")
;	PRINTLC [31] ???
;	RESETCOLOR
;ENDIF


;--------------------------------------------------
;33,ゾウシガヤ霊園,クリア条件複雑,ダンジョンフラグ:1:1 == 1,不明
;--------------------------------------------------
@DIARY_DUNGEON_33
;出現フラグのチェック　エコービルクリア後
IF ダンジョンフラグ:1:1 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_33
	IF ダンジョンフラグ:1:1 != 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [33] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [33] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [33] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;34,初台シェルター
;--------------------------------------------------
@DIARY_DUNGEON_34
;出現フラグのチェック　無条件
;IF ダンジョンフラグ:1:1 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_34
	IF ダンジョンフラグ:34:8 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [34] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [34] %RESULTS, 20, LEFT%
	ENDIF
;ELSE
;出現していない
;	SETCOLOR COLOR("gray")
;	PRINTLC [33] ???
;	RESETCOLOR
;ENDIF

;--------------------------------------------------
;35,T.D.L.,
;--------------------------------------------------
@DIARY_DUNGEON_35
;出現フラグのチェック　
IF ダンジョンフラグ:[[ダンジョン:T.D.L.]]:0
	;出現している
	;クリアフラグのチェック　マルチエンディングかつ報告が必要なタイプ
	;複雑なんで自信が無いけど多少はね
	;エキドナに協力し、カテドラル侵攻受諾フラグを立てた場合、クリア
	;エキドナを倒した場合、クリア
	CALLFORM GET_DUNGEON_NAME_35
	IF ダンジョンフラグ:[[ダンジョン:T.D.L.]]:1 == 4 || ダンジョンフラグ:[[ダンジョン:T.D.L.]]:1 == -1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [35] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [35] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [35] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;36,原宿シェルター
;--------------------------------------------------
@DIARY_DUNGEON_36
;出現フラグのチェック　キャラチェックがあるタイプ
;イベントフラグでルート管理をしている 0でも99でもなく、かつキャラがいることが条件
;クリアフラグはダンジョンフラグでも立ててくれているのでそちらを使う
IF (イベントフラグ:70:0 != 0 && イベントフラグ:70:0 != 99 && (FINDCHARA_B(4761,0) == 2)) || ダンジョンフラグ:36:1 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_36
	IF ダンジョンフラグ:36:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [36] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [36] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [36] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;37,シャンシャンシティ
;--------------------------------------------------
@DIARY_DUNGEON_37
;出現フラグのチェック　キャラチェックがあるタイプ
;イベントフラグでルート管理をしている 2以上かつ99でない かつキャラがいることが条件
;クリアフラグはダンジョンフラグでも立ててくれているのでそちらを使う
IF (イベントフラグ:70:0 >= 2 && イベントフラグ:70:0 != 99 && (FINDCHARA_B(4761,0) == 2)) || ダンジョンフラグ:37:1 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_37
	IF ダンジョンフラグ:37:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [37] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [37] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [37] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;38,御茶ノ水シェルター
;--------------------------------------------------
@DIARY_DUNGEON_38
;出現フラグのチェック　
IF イベントフラグ:70:1 == 1 || イベントフラグ:70:1 >= 2
	;出現している
	;クリアフラグのチェック イベントで管理
	CALLFORM GET_DUNGEON_NAME_38
	IF イベントフラグ:70:1 >= 2
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [38] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [38] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [38] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;39,黄泉
;--------------------------------------------------
@DIARY_DUNGEON_39
;出現フラグのチェック　キャラチェックがあるタイプ
;イベントフラグでルート管理をしている 7以上かつ99でない かつキャラがいることが条件
;クリアフラグはダンジョンフラグでも立ててくれているのでそちらを使う
IF  (イベントフラグ:70:0 >= 7 && イベントフラグ:70:0 != 99 && (FINDCHARA_B(4761,0) == 2)) || ダンジョンフラグ:39:1 == 1
	;出現している
	;クリアフラグのチェック デカラビア撃破
	CALLFORM GET_DUNGEON_NAME_39
	IF ダンジョンフラグ:39:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [39] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [39] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [39] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;40,市ヶ谷駐屯地
;--------------------------------------------------
@DIARY_DUNGEON_40
;出現フラグのチェック　マヨーネ様からの依頼を受けている、マヨーネ様を手に入れている
IF イベントフラグ:8:0 == 1 || イベントフラグ:8:0 == 3 || イベントフラグ:1:0 == 3
	;出現している
	;クリアフラグのチェック 
	CALLFORM GET_DUNGEON_NAME_40
	IF ダンジョンフラグ:40:23 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [40] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [40] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [40] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;41,(某国大使館)アメリカ大使館
;--------------------------------------------------
@DIARY_DUNGEON_41
;出現フラグのチェック　マヨーネ様からの依頼、マヨーネ様を手に入れている
IF イベントフラグ:8:0 == 3 || イベントフラグ:8:0 == 3 || イベントフラグ:1:0 == 3
	;出現している
	;クリアフラグのチェック 
	CALLFORM GET_DUNGEON_NAME_41
	IF ダンジョンフラグ:41:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [41] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [41] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [41] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;42,新宿,
;--------------------------------------------------
@DIARY_DUNGEON_42
;出現フラグのチェック　新宿衛生病院クリア
IF ダンジョンフラグ:3:1 == 1
	;出現している
	;クリアフラグのチェック
	;暫定的に各ルートの4以上
	CALLFORM GET_DUNGEON_NAME_42
	IF FLAG:百合の花ルート >= 4 || FLAG:直哉ルート >= 4
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [42] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [42] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [41] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;43,貪欲界
;--------------------------------------------------
@DIARY_DUNGEON_43
;出現フラグのチェック　
IF (イベントフラグ:2:0 == 1 && BASE:MASTER:LV > 30) || ダンジョンフラグ:43:0 == 1
	;出現している
	;クリアフラグのチェック チェフェイ撃破
	CALLFORM GET_DUNGEON_NAME_43
	IF ダンジョンフラグ:43:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [43] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [43] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [43] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;44,ハザマの精神世界（ボクのおちんちんらんど）
;--------------------------------------------------
@DIARY_DUNGEON_44
;出現フラグのチェック　本来は周回すると消えるタイプだが、周回プレイだと消えない
;初回レイコPT内、軽子坂上層クリア、貪欲界クリアが条件
;クリア済み
;もしくは未クリアで諸条件を満たしレイコがPT内にいる場合のみ表示
IF ダンジョンフラグ:44:0 == 1 || (FINDCHARA_B(4508) == 2 && ダンジョンフラグ:7:1 == 1 && ダンジョンフラグ:43:0 == 1 && ダンジョンフラグ:44:0 == 0)
	;出現している
	;クリアフラグのチェック ハザマ撃破
	CALLFORM GET_DUNGEON_NAME_44
	IF ダンジョンフラグ:44:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [44] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [44] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [44] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;45,六本木
;--------------------------------------------------
@DIARY_DUNGEON_45
;出現フラグのチェック　
IF ダンジョンフラグ:18:22 == 1 || ダンジョンフラグ:45:0 >= 1
	;出現している
	;クリアフラグのチェック ピラー入手
	;迷ったけどピラーとアリスを両方入手でクリア扱いに
	CALLFORM GET_DUNGEON_NAME_45
	IF ダンジョンフラグ:45:0 >= 1 && ダンジョンフラグ:45:2 ==3
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [45] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [45] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [45] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;46,新宿大聖堂
;--------------------------------------------------
@DIARY_DUNGEON_46
;出現フラグのチェック　
IF ダンジョンフラグ:18:22 == 1 && ダンジョンフラグ:42:0 >= 1
	;出現している
	;クリアフラグのチェック パズスとごにゃごにゃした
	CALLFORM GET_DUNGEON_NAME_46
	IF ダンジョンフラグ:46:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [46] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [46] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [46] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;47,品川,
;--------------------------------------------------
@DIARY_DUNGEON_47
;出現フラグのチェック　
IF ダンジョンフラグ:[[ダンジョン:品川]]:0
	;出現している
	;クリアフラグのチェック　マルチエンディングかつ報告が必要なタイプ
	;複雑なんで自信が無いけど多少はね
	;ハニエルに協力し、カテドラル受諾フラグを立てた場合、クリア
	;ハニエルを倒した場合、クリア
	CALLFORM GET_DUNGEON_NAME_47
	IF ダンジョンフラグ:[[ダンジョン:品川]]:1 == 4 || ダンジョンフラグ:[[ダンジョン:品川]]:1 == -1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [47] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [47] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [47] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;48,カーサ乾
;--------------------------------------------------
@DIARY_DUNGEON_48
;出現フラグのチェック　クリアすると消えるタイプ
IF 依頼フラグ:5:0 == 1 || ダンジョンフラグ:48:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_48
	IF ダンジョンフラグ:48:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [48] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [48] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [48] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;49,北山大学
;--------------------------------------------------
@DIARY_DUNGEON_49
;出現フラグのチェック　クリアすると消えるタイプ
IF (依頼フラグ:5:0 == 1 && BASE:MASTER:LV >= 12) || ダンジョンフラグ:49:0 == 1
	;出現している
	;クリアフラグのチェック　シキオウジ撃破
	CALLFORM GET_DUNGEON_NAME_49
	IF ダンジョンフラグ:49:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [49] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [49] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [49] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;50,七姉妹学園
;--------------------------------------------------
@DIARY_DUNGEON_50
;出現フラグのチェック　クリアすると消えるタイプ STRFLAG関数がいまいち分からない
IF STRFLAG_REQ("七姉妹学園調査依頼クリア", 1, [[依頼:ペルソナ２]])
	;出現している
	;クリアフラグのチェック　自信がない
	CALLFORM GET_DUNGEON_NAME_50
	IF STRFLAG_D("七姉妹学園クリア", 1, [[ダンジョン:ペルソナ２]])
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [50] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		CALLF STRFLAG_D("七姉妹学園クリア", -1, [[ダンジョン:ペルソナ２]])
		PRINTFORMLC [50] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	CALLF STRFLAG_REQ("七姉妹学園調査依頼クリア", -1, [[依頼:ペルソナ２]])
	SETCOLOR COLOR("gray")
	PRINTLC [50] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;51,ゾディアック
;--------------------------------------------------
@DIARY_DUNGEON_51
;出現フラグのチェック　クリアすると消えるタイプ
IF STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]]) >= 3
	;出現している
	;クリアフラグのチェック　これでいいのか不安
	CALLFORM GET_DUNGEON_NAME_51
	IF STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]]) >= 4
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [51] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [51] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [51] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;52,空の科学館
;--------------------------------------------------
@DIARY_DUNGEON_52
;出現フラグのチェック　クリアすると消えるタイプ
IF STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]]) >= 7 || STRFLAG_NUM_EV("罰ルート進行", , , , [[イベント:ペルソナ２]]) >= 5
	;出現している
	;クリアフラグのチェック　これでいいのか不安
	CALLFORM GET_DUNGEON_NAME_52
	IF STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]]) >= 8 || STRFLAG_NUM_EV("罰ルート進行", , , , [[イベント:ペルソナ２]]) >= 6
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [52] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [52] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [52] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;53,カラコル
;--------------------------------------------------
@DIARY_DUNGEON_53
;出現フラグのチェック　クリアすると消えるタイプ
IF STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]]) >= 10
	;出現している
	;クリアフラグのチェック　これでいいのか不安
	CALLFORM GET_DUNGEON_NAME_53
	IF STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]]) >= 11
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [53] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [53] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [53] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;54,森本病院
;--------------------------------------------------
@DIARY_DUNGEON_54
;出現フラグのチェック　クリアすると消えるタイプ
IF STRFLAG_NUM_EV("罰ルート進行", , , , [[イベント:ペルソナ２]]) >= 3
	;出現している
	;クリアフラグのチェック　これでいいのか不安
	CALLFORM GET_DUNGEON_NAME_54
	IF STRFLAG_NUM_EV("罰ルート進行", , , , [[イベント:ペルソナ２]]) >= 4
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [54] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [54] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [54] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;55,シバルバー
;--------------------------------------------------
@DIARY_DUNGEON_55
;出現フラグのチェック　クリアすると消えるタイプ
IF STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]]) >= 12
	;出現している
	;クリアフラグのチェック　これでいいのか不安
	CALLFORM GET_DUNGEON_NAME_55
	IF STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]]) >= 13
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [55] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [55] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [55] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;59,バール城
;--------------------------------------------------
@DIARY_DUNGEON_59
;出現フラグのチェック　クリアすると消えるタイプ
;キャラチェックがある
;ＥＤをキャンセルした場合のフラグ管理が無いので、キャンセル済みEDで代用
;不安
;ファイル名とダンジョン名変えるのやめてぇ！不安になるぅ！
IF (イベントフラグ:70:0 == 10 && FINDCHARA_B(4761,0) == 2 && (FINDCHARA_B(4763,0) == 2 || FINDCHARA_B(4764,0) == 2)) || (イベントフラグ:70:0 == 11 && FINDCHARA_B(4761,0) == 2) || イベントフラグ:70:0 == 12 ||  GETBIT(FLAG:キャンセル済みED,16) == 1
	;出現している
	;クリアフラグのチェック
	;ＥＤをキャンセルした場合のフラグ管理が無いので、キャンセル済みEDで代用
	CALLFORM GET_DUNGEON_NAME_59
	IF イベントフラグ:70:0 == 12 || GETBIT(FLAG:キャンセル済みED,16) == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [59] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [59] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [59] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;60,アヴィディア界
;--------------------------------------------------
@DIARY_DUNGEON_60
;出現フラグのチェック　クリアすると消えるタイプ
;キャンセル済みED,28も判定として使うことに
IF ダンジョンフラグ:19:1 == 1 || GETBIT(FLAG:キャンセル済みED,28) == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_60
	IF ダンジョンフラグ:60:0 == 1 || GETBIT(FLAG:キャンセル済みED,28) == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [60] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [60] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [60] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;61,原宿
;--------------------------------------------------
@DIARY_DUNGEON_61
;出現フラグのチェック
;私家版対応済み 無条件
;IF 依頼フラグ:12:0 != 0
	;出現している
	;クリアフラグのチェック
	;私家版対応済み
	CALLFORM GET_DUNGEON_NAME_61
	IF ダンジョンフラグ:61:0 >= 5
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [61] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [61] %RESULTS, 20, LEFT%
	ENDIF
;ELSE
;出現していない
;	SETCOLOR COLOR("gray")
;	PRINTLC [61] ???
;	RESETCOLOR
;ENDIF

;--------------------------------------------------
;62,アイスランド
;--------------------------------------------------
@DIARY_DUNGEON_62
;出現フラグのチェック　イベントクリア
IF 依頼フラグ:12:0 == 1 || ダンジョンフラグ:62:1 == 4
	;出現している
	;クリアフラグのチェック　ヘル撃破
	CALLFORM GET_DUNGEON_NAME_62
	IF ダンジョンフラグ:62:1 == 4
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [62] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [62] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [62] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;63,サンドランド
;--------------------------------------------------
@DIARY_DUNGEON_63
;出現フラグのチェック フォレストランドにいけることをクリア条件とする
IF 闘技場フラグ:9:0 == 2 || ダンジョンフラグ:63:0 == 8
	;出現している
	;クリアフラグのチェック　ヘル撃破
	CALLFORM GET_DUNGEON_NAME_63
	IF ダンジョンフラグ:63:0 == 8
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [63] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [63] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [63] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;64,セントラルランド
;--------------------------------------------------
@DIARY_DUNGEON_64
;出現フラグのチェック
;ダンジョンフラグを立てているか、キャンセル済みED60,61のどちらかをクリア条件とする
IF イベントフラグ:26:0 == 1 || ダンジョンフラグ:64:0 == 6 || GETBIT(FLAG:キャンセル済みED,60) == 1 || GETBIT(FLAG:キャンセル済みED,61) == 1
	;出現している
	;クリアフラグ ダンジョンクリア、もしくはエンディングを見て、続けるを選んだ
	CALLFORM GET_DUNGEON_NAME_64
	IF ダンジョンフラグ:64:0 == 6 || GETBIT(FLAG:キャンセル済みED,60) == 1 || GETBIT(FLAG:キャンセル済みED,61) == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [64] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [64] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [64] ???
	RESETCOLOR
ENDIF



;--------------------------------------------------
;65,シュバルツバース
;--------------------------------------------------
@DIARY_DUNGEON_65
;出現フラグのチェック　イベントクリア
IF イベントフラグ:11:0 == 1
	;出現している
	;クリアフラグのチェック　明らかに未完成じゃないかなぁこれ
	CALLFORM GET_DUNGEON_NAME_65
	IF STRFLAG_NUM_D("シュバルツバース進行度") >= 4
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [65] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [65] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [65] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;66,異界化エルミン学園
;--------------------------------------------------
@DIARY_DUNGEON_66
;出現フラグのチェック クリアすると消えるタイプ
IF イベントフラグ:23:0 >= 2 || ダンジョンフラグ:66:1 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_66
	IF ダンジョンフラグ:66:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [66] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [66] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [66] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;70,Tartarus
;--------------------------------------------------
@DIARY_DUNGEON_70
;出現フラグのチェック 夜じゃないと出ないけどここでは関係ない 数値的に処理
IF イベントフラグ:9:0 >= 1 || ダンジョンフラグ:70:1 >= 10
	;出現している
	;クリアフラグのチェック ニュクスアバター撃破でクリアとする
	;現状最高値が10で数値的に処理されているが、念の為10以上にしておく
	;エリザベス撃破はダンジョンフラグ:70:96 == 4　現在は判定外
	CALLFORM GET_DUNGEON_NAME_70
	IF ダンジョンフラグ:70:1 >= 10
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [70] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [70] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [70] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;72,雪子姫の城
;--------------------------------------------------
@DIARY_DUNGEON_72
;出現フラグのチェック 数値的に処理 
IF イベントフラグ:12:0 >= 1 || ダンジョンフラグ:72:8 >= 1
	;出現している
	;クリアフラグのチェック マルチエンディングだが1以上でクリア
	CALLFORM GET_DUNGEON_NAME_72
	IF ダンジョンフラグ:72:8 >= 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [72] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [72] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [72] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;73,熱気立つ大浴場
;--------------------------------------------------
@DIARY_DUNGEON_73
;出現フラグのチェック 数値的に処理
;クリアすると消える？　ＥＲＢ上での処理は72と同じに見える
IF イベントフラグ:12:0 >= 2 || ダンジョンフラグ:73:8 >= 1
	;出現している
	;クリアフラグのチェック 裏ボス倒すとクリアフラグが2になる
	CALLFORM GET_DUNGEON_NAME_73
	IF ダンジョンフラグ:73:8 >= 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [73] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [73] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [73] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;74,天上楽土
;--------------------------------------------------
@DIARY_DUNGEON_74
;出現フラグのチェック 数値的に処理
;フラグ管理が分かりやすくてありがてえなーおれもなー
IF イベントフラグ:12:0 == 4 || ダンジョンフラグ:74:1 >= 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_74
	IF ダンジョンフラグ:74:1 >= 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [74] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [74] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [74] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;75,禍津稲羽市
;--------------------------------------------------
@DIARY_DUNGEON_75
;出現フラグのチェック 主人公レベルと大浴場
;IF BASE:MASTER:LV > 50 && ダンジョンフラグ:73:8 > 0
;私家版だと天上楽土クリアが条件
;フラグ管理が分かりやすくてありがてえなーおれもなー
IF イベントフラグ:12:0 == 5 || ダンジョンフラグ:75:1 == 2
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_75
	IF ダンジョンフラグ:75:1 == 2
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [75] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [75] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [75] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;78,氷川神社
;--------------------------------------------------
@DIARY_DUNGEON_78
;出現フラグのチェック 北山大学クリア　クリアすると消えるタイプ
;IF ダンジョンフラグ:49:0 == 1
;私家版では依頼のクリアが条件に変更
;マルチエンディング(1,2)になっているのでそれにも対応
IF 依頼フラグ:5:0 == 2 || ダンジョンフラグ:78:0 >= 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_78
	IF ダンジョンフラグ:78:0 >= 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [78] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [78] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [78] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;79,東亜テレビ
;--------------------------------------------------
@DIARY_DUNGEON_79
;出現フラグのチェック 神社クリア クリアすると消えるタイプ
;IF ダンジョンフラグ:78:0 == 1
;出現フラグが変更されている
IF ダンジョンフラグ:86:0 == 1 || ダンジョンフラグ:79:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_79
	IF ダンジョンフラグ:79:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [79] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [79] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [79] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;80,偽見滝原
;--------------------------------------------------
@DIARY_DUNGEON_80
;出現フラグのチェック 私家版新規ダンジョン
;クリア条件として、イベントフラグ一定以上もしくはキャンセル済みエンドで判定
;キャンセル済みED,18
IF イベントフラグ:99:0 == 8 || イベントフラグ:99:0 >= 9 || GETBIT(FLAG:キャンセル済みED,18) == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_80
	IF イベントフラグ:99:0 >= 9 ||  GETBIT(FLAG:キャンセル済みED,18) == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [80] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [80] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [80] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;81,新市庁舎
;--------------------------------------------------
@DIARY_DUNGEON_81
;出現フラグのチェック 私家版新規ダンジョン
;クリアすると消えるタイプ
IF ダンジョンフラグ:84:0 == 1 || ダンジョンフラグ:81:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_81
	IF ダンジョンフラグ:81:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [81] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [81] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [81] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;82,異界化ビル
;--------------------------------------------------
@DIARY_DUNGEON_82
;出現フラグのチェック 依頼受諾済み クリアすると消えるタイプ
IF 依頼フラグ:29:0 >= 1
	;出現している
	;クリアフラグのチェック こっちはダンジョンのクリア判定を使う
	;実質依頼フラグ == 2でも同じ
	CALLFORM GET_DUNGEON_NAME_82
	IF ダンジョンフラグ:82:5 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [82] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [82] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [82] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;83,平崎警察署
;--------------------------------------------------
@DIARY_DUNGEON_83
;出現フラグのチェック 東亜テレビクリア済み クリアすると消えるタイプ
IF ダンジョンフラグ:79:0 == 1 || ダンジョンフラグ:83:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_83
	IF ダンジョンフラグ:83:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [83] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [83] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [83] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;84,天堂天山邸
;--------------------------------------------------
@DIARY_DUNGEON_84
;出現フラグのチェック 依頼受諾済み クリアすると消える
;IF 依頼フラグ:32:0 >= 1
;私家版だと警察署クリアが出現条件　クリアすると消えるタイプ
IF ダンジョンフラグ:83:0 == 1 || ダンジョンフラグ:84:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_84
	IF ダンジョンフラグ:84:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [84] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [84] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [84] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;85,古墳迷宮
;--------------------------------------------------
@DIARY_DUNGEON_85
;出現フラグのチェック 私家版での追加ダンジョン 出現フラグが複雑
;新市庁舎クリア済みで未クリア　かつ
;	久美子を助けていて陥落済み
;	もしくは久美子を助けていない
;クリアすると消えるタイプ
;PTに存在しないキャラクターに対する陥落チェックがかなり面倒
;しょうがないので入れ子のＩＦ文を２回走らせるクソ処理
CALLFORM GET_DUNGEON_NAME_85
;まずはダンジョンのフラグチェック
IF ダンジョンフラグ:85:0 == 1
	;クリア済み
	SETCOLOR COLOR("light-gray")
	PRINTFORMLC [85] %RESULTS, 20, LEFT%
	;古墳迷宮
	RESETCOLOR
ELSEIF	ダンジョンフラグ:78:0 == 1 && ダンジョンフラグ:85:0 == 0
	;久美子救出ルート
	LOCAL = GETCHARA([[キャラ:久美子]])
	IF ダンジョンフラグ:78:1 == 1 && FINDCHARA_B([[キャラ:久美子]]) == 1 && 陥落(LOCAL)
		;出現条件を満たしていて未クリア
		PRINTFORMLC [85] %RESULTS, 20, LEFT%
		;古墳迷宮
	ELSE
		;出現条件を満たしてない
		SETCOLOR COLOR("gray")
		PRINTLC [85] ???
		RESETCOLOR
	ENDIF
ELSEIF	ダンジョンフラグ:78:0 == 2 && ダンジョンフラグ:85:0 == 0
	;出現条件を満たしていて未クリア
	PRINTFORMLC [85] %RESULTS, 20, LEFT%
	;古墳迷宮
ELSE
	;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [85] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;86,博物館
;--------------------------------------------------
@DIARY_DUNGEON_86
;出現フラグのチェック 私家版での追加ダンジョン
;市庁舎クリア済み クリアすると消えるタイプ
IF ダンジョンフラグ:78:0 >= 1 || ダンジョンフラグ:86:0 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_86
	IF ダンジョンフラグ:86:0 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [86] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [86] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [86] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;88,レオン自動車工場
;--------------------------------------------------
@DIARY_DUNGEON_88
;出現フラグのチェック 私家版での追加ダンジョン
;フラグ管理が分かりやすく書いてくれて助かるなーおれもなー
IF (イベントフラグ:27:0 == 1 && ダンジョンフラグ:88:1 == 0) || ダンジョンフラグ:88:1 == 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_88
	IF ダンジョンフラグ:88:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [88] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [88] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [88] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;89,天体博物館
;--------------------------------------------------
@DIARY_DUNGEON_89
;出現フラグのチェック 依頼受諾以上 数値的に管理
IF 依頼フラグ:33:0 >= 1
	;出現している
	;クリアフラグのチェック マルチエンディング 数値的に管理 0のみ未クリア
	CALLFORM GET_DUNGEON_NAME_89
	IF ダンジョンフラグ:89:1 >= 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [89] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [89] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [89] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;90,ＶＲ美術館
;--------------------------------------------------
@DIARY_DUNGEON_90
;出現フラグのチェック クリアすると消滅するタイプ
IF BASE:MASTER:LV >= 15
	;出現している
	;クリアフラグのチェック スナッピーに懐柔されるエンドでもクリアとする
	CALLFORM GET_DUNGEON_NAME_90
	IF ダンジョンフラグ:90:1 >= 3
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [90] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [90] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [90] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;92,ＶＲホラー・ハウス
;--------------------------------------------------
@DIARY_DUNGEON_92
;出現フラグのチェック
;処理上は90:1が3のときしか開かないが、
;VR美術館では4でも開けるメッセージを出しているので準ずる
;IF ダンジョンフラグ:90:1 >= 3
;私家版では美術館の懐柔ＥＤに対応済み
IF (ダンジョンフラグ:90:1 >= 3 && ダンジョンフラグ:92:0 == 0) || ダンジョンフラグ:92:0 >= 1
	;出現している
	;クリアフラグのチェック
	CALLFORM GET_DUNGEON_NAME_92
	IF ダンジョンフラグ:92:0 >= 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [92] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [92] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [92] ???
	RESETCOLOR
ENDIF


;--------------------------------------------------
;93,天海空港
;--------------------------------------------------
@DIARY_DUNGEON_93
;出現フラグのチェック
IF 依頼フラグ:34:0 == 1 || ダンジョンフラグ:93:1 >= 4
	;出現している
	;クリアフラグのチェック  マルチエンディングで数値的に管理 4/5がクリアフラグ
	CALLFORM GET_DUNGEON_NAME_93
	IF ダンジョンフラグ:93:1 >= 4
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [93] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [93] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [93] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;94,シーアーク
;--------------------------------------------------
@DIARY_DUNGEON_94
;出現フラグのチェック
;クリアとしても未クリアとしても混乱を招くので、関数は作るが表示しない
;つまり未実装処理の対象
IF ダンジョンフラグ:94:1 >= 1
	;出現している
	CALLFORM GET_DUNGEON_NAME_94
	IF ダンジョンフラグ:94:1 > 10
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [94] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [94] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [94] ???
	RESETCOLOR
ENDIF

;--------------------------------------------------
;95,二上門地下
;--------------------------------------------------
@DIARY_DUNGEON_95
;出現フラグのチェック 私家版での新規ダンジョン
;クリアすると消えるタイプ
;読み方が分かりません　ふ・・・ふたなりもんちか？
IF 依頼フラグ:35:0 == 1 || ダンジョンフラグ:95:0 == 2
	;出現している クリアフラグは数値的に処理
	CALLFORM GET_DUNGEON_NAME_95
	IF ダンジョンフラグ:95:0 == 2
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTFORMLC [95] %RESULTS, 20, LEFT%
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTFORMLC [95] %RESULTS, 20, LEFT%
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [95] ???
	RESETCOLOR
ENDIF




;--------------------------------------------------
;追記用のサンプルスクリプト
;--------------------------------------------------

;--------------------------------------------------
;S,ダンジョンの名前,備考,出現フラグ,クリアフラグ
;--------------------------------------------------
@DIARY_DUNGEON_S
;出現フラグのチェック　
IF ダンジョンフラグ:1:1 != 0
	;出現している
	;クリアフラグのチェック
	IF ダンジョンフラグ:3:1 == 1
	;クリア済み
		SETCOLOR COLOR("light-gray")
		PRINTLC [ s] Name of Dungeon
		RESETCOLOR
	ELSE
	;出現しているが未クリア
		PRINTLC [ s] Name of Dungeon
	ENDIF
ELSE
;出現していない
	SETCOLOR COLOR("gray")
	PRINTLC [ s] ???
	RESETCOLOR
ENDIF

