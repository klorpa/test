;--------------------------------------------------
;セーブデータ互換処理
;--------------------------------------------------
@ISIMTERCHANGEABILITY_K4518
;依存度が0でない、かつ、バージョン0.1未満
;ちょっとでも調教してれば依存度が0であることはまずめったに無いだろうということで
;調教状況のみ白紙化可能
;引継ぎ処理考えたらいらないなぁという気分に
IF CFLAG:2 && CFLAG:501 < 10
;	PRINTL すでに作成された星 あかりをRPG部分での能力そのままに
;	PRINTL 調教されていない状況に戻すことができます
;	PRINTL 初期化処理をしますか？
;	PRINTL [0] はい
;	PRINTL [1] いいえ
;	CALL INPUTINT(0,1)
;	IF RESULT == 0
;		CALL RESET_CHARA_TRAIN
;		NAME:TARGET = 少女
;		CALLNAME:TARGET = 少女
;		PRINTW 初期化処理を実行しました
;	ELSE
;		PRINTW 初期化処理をせずに進みます
;	ENDIF
ELSEIF CFLAG:501 < 20
	TALENT:一線越えない = 0
ELSEIF CFLAG:591 < 291
	CALL SET_EVENTFLAG(9, 1)
ENDIF
;--------------------------------------------------
;バージョン取得
;セーブデータ互換を切る場合に備える
;バージョンさえとっておけば、互換処理の作りやすさが段違いなので
;--------------------------------------------------
CFLAG:501 = 300
