
;======================================================
;‘•”υ•i‚Μƒf[ƒ^
;¶“Ύ•ν
;Innate weapon
;======================================================


@ξ–{”\—ΝC³_2093,ARG,ARG:1 = -1
SELECTCASE ARG
ENDSELECT
RETURN 0


@ν“¬”\—ΝC³_2093,ARG,ARG:1 = -1
IF ARG:1 == -1
	SELECTCASE ARG
		CASE 0
			RETURN 24
		CASE 1
			RETURN 31
		CASE 4
			RETURN 20
		CASE 5
			RETURN 8
	ENDSELECT
ELSE
	SELECTCASE ARG
		CASE 0
			RETURN (MAXBASE:(ARG:1):LV)*3/2 + MAXBASE:(ARG:1):—Ν * 3/ 2 + MAXBASE:(ARG:1):‘Ο—Ν / 2
		CASE 1
			RETURN (MAXBASE:(ARG:1):LV) + MAXBASE:(ARG:1):‘¬‚³ * 3/ 2 + MAXBASE:(ARG:1):‰^ / 2
		CASE 4
			RETURN (MAXBASE:(ARG:1):LV)*3/2 + MAXBASE:(ARG:1):–‚—Ν * 3/ 2 + MAXBASE:(ARG:1):’mb / 2
		CASE 5
			RETURN ((MAXBASE:(ARG:1):LV) + MAXBASE:(ARG:1):’mb + MAXBASE:(ARG:1):–‚—Ν / 2 + MAXBASE:(ARG:1):‰^ / 2)/2
	ENDSELECT
ENDIF
RETURN 0

@–hδ‘«_2093,ARG,ARG:1 = -1
SELECTCASE ARG
ENDSELECT
RETURN 100

@U‚‘«_2093, ARG = -1
RETURN 3

@Ε’αU‚‰ρ”_2093, ARG = -1
RETURN 1

@Ε‘εU‚‰ρ”_2093, ARG = -1
RETURN 1

@Λ’φ_2093, ARG = -1
RETURN 1

@U‚”ΝΝ_2093, ARG = -1
RETURN 1

@‘•”υ‰Σ_2093
RETURN GET_EQUIPNUM("•")

;-----------------------------
;- ƒVƒ‡ƒbƒv
;-----------------------------
@w“όπ_2093
RETURN 0

@•K—vƒƒxƒ‹_2093
RETURN 1

@‘•”υπ_2093,ARG
SIF ABL:ARG:ν‘° == 36 && (NO:ARG == [[ƒLƒƒƒ‰:ƒ†[ƒƒA]] || TALENT:ARG:ηκ’ά || TALENT:ARG:‰H || TALENT:ARG:ηκ‘« || TALENT:ARG:”φ || TALENT:ARG:{ || TALENT:ARG:Gθ || TALENT:ARG:b || TALENT:ARG:’Ή || TALENT:ARG:ΰ¦’—ή || TALENT:ARG:‹›)
	RETURN 1
RETURN 0

;ΰ–Ύ‚Ν–³‚µ