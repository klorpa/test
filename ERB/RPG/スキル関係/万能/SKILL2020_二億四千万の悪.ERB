;

;スキル名
@SKILL_NAME_2020,ARG
RESULTS = 240 000 000 Evils

;　命中
@SKILL_HITRATE_2020, ARG
RETURN 120

;　威力
@SKILL_POWER_2020, ARG
RETURN 500

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_2020,ARG
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_2020,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2020, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_2020,ARG
RETURN 36

;　相性
@SKILL_TYPE_2020,ARG
RETURN 17
;Almig

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2020
RETURN 1
;魔法

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_2020
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_2020
RETURN 4

;　射程
@SKILL_RANGE_2020,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_2020,ARG
RETURN 4

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_2020
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2020,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_2020
RETURN 20

@継承不能_2020
RETURN 1
;Almig

;速度補正
@SKILL_SPEED_2020, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_2020,ARG


;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_2020
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_2020
RETURN 0
;変化不能

;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_2020,ARG
RETURN 0

;追加効果相性
@SKILL_ADDTIONAL_TYPE_2020,ARG
RETURN 0

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_2020,ARG
RETURN 0

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_2020,ARG
RETURN 0
;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_2020,ARG,ARG:1
;CALL PAY_COST,ARG,2020
FOR LOCAL,1,17
	LOCALS = ポジション{LOCAL}
	SIF FLAG:LOCALS < 0 || FLAG:LOCALS == ARG
		CONTINUE
	SIF ABL:(FLAG:LOCALS):属性LC != 2 && ABL:(FLAG:LOCALS):属性LD != 2
		CONTINUE
	SIF GET_STATE(CFLAG:(FLAG:LOCALS):ステート) == "DYING"
		CONTINUE
	CALLFORM ATTACK_SINGLE,ARG,FLAG:LOCALS,2020
NEXT





;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_2020
PRINTL Liberates Ahriman's power to deal severe Almighty damage to all Neutral demons.

;継承に必要な部位
@継承部位_2020,ARG
RETURN 1

;使用条件
@SKILL_SPECIAL_ACTIONABLE_2020,ARG
RETURN 1
;とくにない

