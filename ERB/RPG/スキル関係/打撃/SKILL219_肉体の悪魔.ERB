
;スキル名
@SKILL_NAME_219,ARG
RESULTS = Diablo De Corps
;肉体の悪魔

;　命中
@SKILL_HITRATE_219, ARG
SIF ARG >= 0 && CFLAG:ARG:ボスフラグ
		RETURN 200
RETURN 100

;　威力
@SKILL_POWER_219, ARG
SIF ARG >= 0 && CFLAG:ARG:ボスフラグ
		RETURN 120
RETURN 90

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_219,ARG
SIF ARG >= 0 && CFLAG:ARG:ボスフラグ
		RETURN 5
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_219,ARG
RETURN 10

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_219, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_219,ARG
RETURN 40

;　相性
@SKILL_TYPE_219,ARG
RETURN 2
;Strke

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_219
RETURN 1
;どちらでもない

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_219
RETURN 1


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_219
RETURN 1

;　射程
@SKILL_RANGE_219,ARG
RETURN 1

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_219,ARG
RETURN 1

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_219
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_219,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_219
RETURN 2
;Strke

;速度補正
@SKILL_SPEED_219, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_219,ARG


;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_219
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_219
RETURN 14

@SKILL_RANKUP_219
;ランクアップ先は１つ
RETURN 0
;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_219,ARG
RETURN GET_STATE_NUM("SHOCK")

;追加効果相性
@SKILL_ADDTIONAL_TYPE_219,ARG
RETURN GET_TYPE_NUM("打撃")

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_219,ARG
SIF ARG >= 0 && CFLAG:ARG:ボスフラグ
		RETURN 50
RETURN 3

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_219,ARG
SIF ARG >= 0 && CFLAG:ARG:ボスフラグ
		RETURN 100
RETURN 6

;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_219,ARG,ARG:1
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,219






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_219
PRINTL Deals light Strike damage 1 to 10 times + chance to SHOCK to a foe.

;継承に必要な部位
@継承部位_219,ARG
RETURN 1


;使用条件
@SKILL_SPECIAL_ACTIONABLE_219,ARG
RETURN 1
;とくにない

