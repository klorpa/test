;ファントム反逆EDが作られた際に最終ボスになるだろうから割とズルい性能にしておく。でも、飛具無効以上の相性とテトラカーンだけは勘弁な！

;スキル名
@SKILL_NAME_156,ARG
RESULTS = Spore Cannon
;異形胞子飛弾
;　命中
@SKILL_HITRATE_156, ARG
RETURN 120

;　威力
@SKILL_POWER_156, ARG
RETURN 140

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_156,ARG
RETURN 5

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_156,ARG
RETURN 10

;一体辺りの最大ＨＩＴ数
@SKILL_MAXATK_PER_156,ARG
RETURN 4

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_156, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_156,ARG
RETURN 40

;　相性
@SKILL_TYPE_156,ARG
RETURN 1
;飛具

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_156
RETURN 1
;どちらでもない

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_156
RETURN 1


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_156
RETURN 1

;　射程
@SKILL_RANGE_156,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_156,ARG
RETURN 3

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_156
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_156,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_156
RETURN 1
;打撃

;速度補正
@SKILL_SPEED_156, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_156,ARG


;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_156
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_156
RETURN 18

@SKILL_RANKUP_156
RETURN 0

;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_156,ARG
A = RAND:8
IF A == 0
	RETURN GET_STATE_NUM("CHARM")
ELSEIF A == 1
	RETURN GET_STATE_NUM("PANIC")
ELSEIF A == 2
	RETURN GET_STATE_NUM("SLEEP")
ELSEIF A == 3
	RETURN GET_STATE_NUM("HAPPY")
ELSEIF A == 4
	RETURN GET_STATE_NUM("BIND")
ELSEIF A == 5
	RETURN GET_STATE_NUM("CLOSE")
ELSEIF A == 6
	RETURN GET_STATE_NUM("POISON")
ELSE
	RETURN GET_STATE_NUM("PALYZE")
ENDIF
;追加効果相性
@SKILL_ADDTIONAL_TYPE_156,ARG
IF A < 4
	RETURN 9
ELSE
	RETURN 8
ENDIF
RETURN 8

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_156,ARG
;ダメージもシャレにならないし、針程度にしておく
RETURN 40

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_156,ARG
RETURN 60

;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_156,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,156






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_156
PRINTL Deals light Gun damage + chance of various status element to random foes.
PRINTL Manitou's special move.

;継承に必要な部位
@継承部位_156,ARG
RETURN 1


;使用条件
@SKILL_SPECIAL_ACTIONABLE_156,ARG
RETURN 1
;とくにない

