;

;スキル名
@SKILL_NAME_2502,ARG
RESULTS = God's Judgement
;神の裁き

;　命中
@SKILL_HITRATE_2502, ARG
RETURN 1000

;　威力
@SKILL_POWER_2502, ARG
RETURN 9999

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_2502,ARG
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_2502,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2502, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_2502,ARG
RETURN 99

;　相性
@SKILL_TYPE_2502,ARG
RETURN GET_TYPE_NUM("万能")

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2502
RETURN 1

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_2502
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_2502
RETURN 1

;　射程
@SKILL_RANGE_2502,ARG
RETURN 2

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_2502,ARG
RETURN 1

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_2502
RETURN 3

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2502,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_2502
RETURN 14

;速度補正
@SKILL_SPEED_2502, ARG = -1
RETURN 99

;　口上の表示とそのタイプ
@SKILL_MESSAGE_2502,ARG


;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_2502
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_2502
RETURN 23


;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_2502,ARG
RETURN GET_STATE_NUM("DYING")

;追加効果相性
@SKILL_ADDTIONAL_TYPE_2502,ARG
RETURN GET_TYPE_NUM("万能")

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_2502,ARG
RETURN 100

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_2502,ARG
RETURN 100
;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_2502,ARG,ARG:1
#LOCALSIZE 1
#DIM CHARA
FOR LOCAL,1,7
	CHARA = POS(LOCAL)
	SIF CHARA < 0 || GROUPMATCH(GET_STATE(CFLAG:CHARA:ステート),"DYING","STONE")
		CONTINUE
	IF EQUIP:CHARA:アクセサリ == 5499
		PRINTFORMW [{LOCAL,2}] %CALLNAME:CHARA,20,LEFT%  >>>>>>  DEBUG-BLOCK
		RETURN
	ENDIF
	PRINTFORM [{LOCAL,2}] %CALLNAME:CHARA,20,LEFT%  >>>>>>  
	CALL BTL_CALC_DAMAGE_EXE(ARG:0, CHARA, 99999, GET_TYPE_NUM("万能"), 2, 0)
NEXT




;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_2502
PRINTL 敵全体に万能属性の不可避極大ダメージ

;継承に必要な部位
@継承部位_2502,ARG
RETURN 1

;使用条件
@SKILL_SPECIAL_ACTIONABLE_2502,ARG
RETURN 1
;とくにない

