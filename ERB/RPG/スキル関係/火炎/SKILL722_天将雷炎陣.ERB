;炎＋電撃の2段に変更
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_722,ARG
RESULTS = Thunder Inferno

;　命中
@SKILL_HITRATE_722, ARG
RETURN 80

;　威力
;COOP取りやすく、追加効果もあるので若干威力低下
@SKILL_POWER_722, ARG
SIF FLAG:攻撃回数 == 0
	RETURN 240
SIF FLAG:攻撃回数 == 1
	RETURN 120

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_722,ARG
RETURN 2

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_722,ARG
RETURN 2

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_722, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_722,ARG
RETURN 45

;　相性
@SKILL_TYPE_722,ARG
SIF FLAG:攻撃回数 == 0
	RETURN GET_TYPE_NUM("火炎")
SIF FLAG:攻撃回数 == 1
	RETURN GET_TYPE_NUM("電撃")
;Fire＋Elect

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_722
RETURN 1
;魔法

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_722
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_722
RETURN 1

;　射程
@SKILL_RANGE_722,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_722,ARG
RETURN 3

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_722
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_722,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_722
RETURN 7

@継承不能_722
RETURN 1
;継承不可

;速度補正
@SKILL_SPEED_722
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_722,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_722
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_722
RETURN 14


;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_722,ARG
RETURN GET_STATE_NUM("SHOCK")

;追加効果相性
@SKILL_ADDTIONAL_TYPE_722,ARG
RETURN GET_TYPE_NUM("電撃")

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_722,ARG
RETURN 15

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_722,ARG
RETURN 95
;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_722,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1, 722





;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_722
PRINTL Deals medium Fire damage + light Electric damage that shock, to all foes. 

;継承に必要な部位
@継承部位_722,ARG
RETURN 1

;使用条件
@SKILL_SPECIAL_ACTIONABLE_722,ARG
RETURN 1
;とくにない

