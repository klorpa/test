;通常攻撃
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_713,ARG
RESULTS = Dark Fiery Fury
;暗黒狂焔乱舞

;　命中
@SKILL_HITRATE_713, ARG
RETURN 90

;　威力
@SKILL_POWER_713, ARG
SIF FLAG:攻撃回数 == 0
	RETURN 115
SIF FLAG:攻撃回数 == 1
	RETURN 100
SIF FLAG:攻撃回数 == 2
	RETURN 80

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_713,ARG
RETURN 3

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_713,ARG
RETURN 3

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_713, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_713,ARG
RETURN 25

;　相性
@SKILL_TYPE_713,ARG
SIF FLAG:攻撃回数 == 0
	RETURN 0
SIF FLAG:攻撃回数 == 1
	RETURN 4
SIF FLAG:攻撃回数 == 2
	RETURN 11
;Fire

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_713
RETURN 1
;魔法

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_713
SIF FLAG:攻撃回数 == 0
	RETURN 1
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_713
RETURN 1

;　射程
@SKILL_RANGE_713,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_713,ARG
RETURN 3

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_713
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_713,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_713
RETURN 7

@継承不能_713
RETURN 1
;継承不可

;速度補正
@SKILL_SPEED_713, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_713,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_713
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_713
RETURN 0


;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_713,ARG
RETURN 0

;追加効果相性
@SKILL_ADDTIONAL_TYPE_713,ARG
RETURN 0

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_713,ARG
RETURN 0

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_713,ARG
RETURN 0
;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_713,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1, 713





;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_713
PRINTL Deals heavy Fire and Drak damage in 3 hits to all foes.

;継承に必要な部位
@継承部位_713,ARG
RETURN 1

;使用条件
@SKILL_SPECIAL_ACTIONABLE_713,ARG
RETURN 1
;とくにない

