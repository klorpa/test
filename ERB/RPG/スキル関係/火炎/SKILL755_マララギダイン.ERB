;通常攻撃
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_755,ARG
RESULTS = Maralagidyne

;　命中
@SKILL_HITRATE_755, ARG
RETURN 90

;　威力
@SKILL_POWER_755, ARG
RETURN 420

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_755,ARG
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_755,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_755, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_755,ARG
RETURN 90

;　相性
@SKILL_TYPE_755,ARG
RETURN 4
;Fire

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_755
RETURN 2
;魔法

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_755
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_755
RETURN 1

;　射程
@SKILL_RANGE_755,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_755,ARG
RETURN 3

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_755
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_755,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_755
RETURN 7

@継承不能_755
RETURN 1
;不可

;速度補正
@SKILL_SPEED_755, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_755,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_755
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_755
RETURN 10


;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_755,ARG
RETURN GET_STATE_NUM("CHARM")

;追加効果相性
@SKILL_ADDTIONAL_TYPE_755,ARG
RETURN 9

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_755,ARG
RETURN 60

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_755,ARG
RETURN 80
;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_755,ARG,ARG:1
;IF NO:ARG == 292
;SETCOLOR 0xFF33FF
;PRINTFORML ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓　
	;PRINTFORM ┃＞%CALLNAME:ARG%は
		;IF RAND:2
			;PRINT そのご立派な偉容をより一層大きく膨張させると
		;ELSE
			;PRINT 自らの脈動する巨体を天へ向けて聳え立たせると
		;ENDIF
		;PRINTFORML %" ",18,LEFT%┃
			;IF RAND:3 == 0
				;PRINTL ┃＞兜にも似た先端から緑色の粘つく液体を発射した！　　　　　　　　　　　　┃
			;ELSEIF RAND:2 == 0
				;PRINTL ┃＞溶岩のようなパトスを辺り一帯にまき散らした！　　　　　　　　　　　　　┃
			;ELSE
				;PRINTL ┃＞噎せ返る臭いと共にドロドロの何かを浴びせ掛けた！　　　　　　　　　　　┃
			;ENDIF
			;PRINTW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;RESETCOLOR
;ENDIF


CALL ATTACK_FIELD,ARG,ARG:1,755





;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_755
PRINTL Deals severe Fire damage + charm to all foes.
PRINTL Finisher that symbolize the demon Mara.


;継承に必要な部位
@継承部位_755,ARG
;男の娘とかふたなりとかその時点で優位なんだから一つくらい純粋な男に利点あってもいいよね？
SIF TALENT:ARG:140
	RETURN 1
RETURN 0

;使用条件
@SKILL_SPECIAL_ACTIONABLE_755,ARG
RETURN 1
;とくにない

