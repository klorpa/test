;

;スキル名
@SKILL_NAME_4805,ARG
RESULTS = Final Call

;　命中
@SKILL_HITRATE_4805, ARG
RETURN 120

;　威力
;相手のライフ割合に応じて威力が格段に上昇する
;マオー・ヘルの体の亀裂に対して放った事の再現？
@SKILL_POWER_4805, ARG
;500 + 自爆者の耐力 * 5 + 発動者の魔力 * 5 + 運 * 2 (最大999)
LOCAL = MIN( 1000 , 999 + BASE:(CFLAG:ARG:リンケージ参加者1):耐力 * 5 + BASE:ARG:魔力 * 5 + BASE:ARG:運*2 )

SELECTCASE ( BASE:(POS(CFLAG:ARG:ターゲット)):ＨＰ * 100 / MAXBASE:(POS(CFLAG:ARG:ターゲット)):ＨＰ ) - 1
CASE IS < 5
	RETURN LOCAL * 10
CASE IS < 10
	RETURN LOCAL * 5
CASE IS < 20
	RETURN LOCAL * 3
CASE IS < 30
	RETURN LOCAL * 2
CASE IS < 40
	RETURN LOCAL * 15 / 10
CASE IS < 50
	RETURN LOCAL * 12 / 10
CASE IS < 90
	RETURN LOCAL
CASEELSE
	PRINTFORMW ELSE
	RETURN LOCAL / 2
ENDSELECT
	

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_4805,ARG
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_4805,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_4805, ARG, ARG:1
RETURN 2
;消費なし＝1

;　コストの量
@SKILL_COST_4805, ARG
RETURN 30

;　相性
@SKILL_TYPE_4805, ARG
VARSET LOCAL
SIF ARG == -1
	RETURN GET_TYPE_NUM("万能")
SIF CFLAG:ARG:ターゲット > 16 || CFLAG:ARG:ターゲット < 1
	RETURN GET_TYPE_NUM("万能")
LOCAL = POS(CFLAG:ARG:ターゲット)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("万能")
LOCAL:1 = MAXBASE:LOCAL:万能 == 999 ? -999 # MAXBASE:LOCAL:万能
SIF CFLAG:LOCAL:万能無効化回数
	LOCAL:1 = 0
LOCAL:2 = MAXBASE:LOCAL:飛具 == 999 ? -999 # MAXBASE:LOCAL:飛具
SIF CFLAG:LOCAL:飛具無効化回数
	LOCAL:2 = 0
RETURN LOCAL:1 > LOCAL:2 ? GET_TYPE_NUM("万能") # GET_TYPE_NUM("飛具")

;　1EXTRAか2MAGICか
;高クリティカル
@SKILL_CRITICAL_RATE_4805
RETURN 30
@SKILL_DECIDE_TYPE_4805
RETURN 1

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_4805
RETURN 1

;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_4805
RETURN 1

;　射程
@SKILL_RANGE_4805,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_4805,ARG
RETURN 1

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_4805
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_4805,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_4805
RETURN 20
;Almig

;速度補正
@SKILL_SPEED_4805, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_4805,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_4805
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_4805
RETURN 0

@SKILL_RANKUP_4805
RETURN 0

;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_4805,ARG
RETURN 0

;追加効果相性
@SKILL_ADDTIONAL_TYPE_4805,ARG
RETURN 0

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_4805,ARG
RETURN 0

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_4805,ARG
RETURN 0
;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_4805, ARG, ARG:1
LOCAL = POS(ARG:1)
LOCAL:1 = CFLAG:ARG:リンケージ参加者1
CALL GUN_SINGLE,ARG,LOCAL,4805
SIF CFLAG:(LOCAL:1):ステート < GET_STATE_NUM("DYING")
	CALL VAR_HP,(LOCAL:1),-BASE:(LOCAL:1):ＨＰ,0

;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_4805
PRINTL 仲魔の全生命力と引き換えに敵1体に飛具＋万能相性特大ダメージ。

;継承に必要な部位
@継承部位_4805,ARG
RETURN 0

;使用条件
@SKILL_SPECIAL_ACTIONABLE_4805,ARG
CALL CHECK_COST,ARG,4805
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない


;=====================================================
;リンケージ設定
;=====================================================
@LINKAGE_4805
RETURN 1

@参加人数_4805
RETURN 1

@参加条件表示_4805, ARG
SELECTCASE ARG
	CASE 1
		PRINTFORM 自爆 or 玉砕破
	CASE 2
		PRINTFORM セツナ or ミライ
ENDSELECT

@発動者条件_4805 , ARG
SIF GROUPMATCH(NO:ARG,[[キャラ:刹那]],[[キャラ:未来]])
	RETURN 1
RETURN 0

@参加者条件_4805, ARG , ARG:1
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
;Humanはダメ
SIF IS_HUMAN(ARG)
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4805, ARG:1
SIF RESULT == 0
	RETURN 0
SELECTCASE ARG:1
	CASE 1
		;自爆系特技を使えること
		IF CHECK_SKILL(ARG, [[スキル:自爆]])
			CALL CHECK_ACTIONABLE , ARG , [[スキル:自爆]]
			SIF RESULT
				RETURN 1
		ENDIF
		IF CHECK_SKILL(ARG, [[スキル:玉砕破]])
			CALL CHECK_ACTIONABLE , ARG , [[スキル:玉砕破]]
			SIF RESULT
				RETURN 1
		ENDIF
		RETURN 0
ENDSELECT

@リンケージコストタイプ_4805, ARG
SELECTCASE ARG
	CASE 1
		RETURN 2
ENDSELECT

@リンケージコスト_4805, ARG
SELECTCASE ARG
	CASE 1
		RETURN 15
ENDSELECT
