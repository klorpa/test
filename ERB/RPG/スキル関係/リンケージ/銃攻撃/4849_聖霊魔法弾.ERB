

;スキル名
@SKILL_NAME_4849,ARG
RESULTS = Holy shot: Soul
;聖霊魔法弾
;　命中
@SKILL_HITRATE_4849, ARG
RETURN 100

;　威力
@SKILL_POWER_4849, ARG
SIF ARG == -1 || CFLAG:ARG:単独リンケージ
	RETURN 90
RETURN 90 + MAXBASE:(CFLAG:ARG:リンケージ参加者1):魔力/2 + MAXBASE:(CFLAG:ARG:リンケージ参加者1):知恵 / 4

;　最低攻撃回数
;マシンガンというかランダムターゲット系は回数が1.5倍になる
@SKILL_MINATTACKNUMBER_4849,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:銃}
IF RESULT == 1
	CALLFORM 最低攻撃回数_{EQUIP:ARG:銃}
	RETURN RESULT * 3 / 2
ELSE
	CALLFORM 最低攻撃回数_{EQUIP:ARG:銃}
	RETURN RESULT
ENDIF

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_4849,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:銃}
IF RESULT == 1
	CALLFORM 最大攻撃回数_{EQUIP:ARG:銃}
	RETURN RESULT * 3 / 2
ELSE
	CALLFORM 最大攻撃回数_{EQUIP:ARG:銃}
	RETURN RESULT
ENDIF
;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_4849, ARG, ARG:1
RETURN 2
;消費なし＝1

;　コストの量
@SKILL_COST_4849, ARG
RETURN 5

;　相性
@SKILL_TYPE_4849,ARG
RETURN GET_TYPE_NUM("破魔")
;　1EXTRAか2MAGICか

@SKILL_DECIDE_TYPE_4849
RETURN 1

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_4849
RETURN 1


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_4849
RETURN 1

;　射程
@SKILL_RANGE_4849,ARG
CALLFORM 射程_{EQUIP:ARG:銃}
RETURN RESULT

;　範囲　1単体　21列　3全体　4　敵味方全体
;　ランダムターゲットでないGunは範囲が一段階分強化される
@SKILL_SPHERE_4849,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:銃}
IF RESULT == 1
	CALLFORM 攻撃範囲_{EQUIP:ARG:銃}
	RETURN RESULT
ELSE
	CALLFORM 攻撃範囲_{EQUIP:ARG:銃}
	RETURN LIMIT(RESULT + 1, 1, 3)
ENDIF
;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_4849
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_4849,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_4849
RETURN 23
;聖

;速度補正
@SKILL_SPEED_4849, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_4849,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_4849
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_4849
RETURN 0

@SKILL_RANKUP_4849
RETURN 0

;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_4849,ARG
RETURN 0

;追加効果相性
@SKILL_ADDTIONAL_TYPE_4849,ARG
RETURN 0

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_4849,ARG
RETURN 0

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_4849,ARG
RETURN 0
;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_4849,ARG,ARG:1
CALL SKILL_SPHERE_4849,ARG
IF RESULT == 1
	LOCAL = POS(ARG:1)
	CALL GUN_SINGLE,ARG,LOCAL,4849
ELSE
	RESULT = 0
	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:銃}
	IF RESULT == 1
		CALL GUN_RAND,ARG,ARG:1,4849
	ELSE
		CALL GUN_FIELD,ARG,ARG:1,4849
	ENDIF
	
ENDIF
SIF CFLAG:ARG:PTフラグ > 0
	ITEM:5503 -= 1





;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_4849
PRINTL Shoot bullets imbued with divine power.

;継承に必要な部位
@継承部位_4849,ARG
RETURN 0

;使用条件
@SKILL_SPECIAL_ACTIONABLE_4849,ARG
CALL CHECK_COST,ARG,4849
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない


;=====================================================
;リンケージ設定
;=====================================================
@LINKAGE_4849
RETURN 1

@参加人数_4849
RETURN 1

@参加条件表示_4849,ARG
SELECTCASE ARG
	CASE 1
		PRINTFORM Megido
	CASE 2
		PRINTFORM Holy Shell
ENDSELECT

@発動者条件_4849 , ARG
SIF EQUIP:ARG:銃 == 0
	RETURN 0
SIF ITEM:5503 < 1
	RETURN 0
TRYCALLFORM 特殊弾倉_{EQUIP:ARG:銃}
FOR LOCAL, 1, RESULT + 1
	LOCALS = 特殊弾{LOCAL}
	SIF EQUIP:ARG:LOCALS == 5503
		RETURN 1
NEXT
RETURN 0


@参加者条件_4849, ARG , ARG:1
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4849, ARG:1
SIF RESULT == 0
	RETURN 0
SELECTCASE ARG:1
	CASE 1
		;メギドを使えること
		IF HAVE_SKILL(ARG, [[スキル:メギド]])
			CALL CHECK_ACTIONABLE , ARG , [[スキル:メギド]]
			SIF RESULT
				RETURN 1
		ENDIF
		RETURN 0
ENDSELECT

@リンケージコストタイプ_4849, ARG
SELECTCASE ARG
	CASE 1
		RETURN 3
ENDSELECT
@リンケージコスト_4849, ARG
SELECTCASE ARG
	CASE 1
		RETURN 4
ENDSELECT
