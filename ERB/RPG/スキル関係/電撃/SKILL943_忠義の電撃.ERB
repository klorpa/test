;消費は1列ダインの2/3、最大火力は同左の0.8倍程度を目安に

;スキル名
@SKILL_NAME_943,ARG
RESULTS = Loyal Shock

;　命中
@SKILL_HITRATE_943, ARG
RETURN 100

;　威力
;最大256(320*0.8)
@SKILL_POWER_943, ARG
SIF ARG < 0
	RETURN 20
RETURN 56 + MIN(BASE:ARG:忠誠度+TCVAR:ARG:獲得忠誠度,3000) / 15

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_943,ARG
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_943,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_943, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_943,ARG
RETURN 13

;　相性
@SKILL_TYPE_943,ARG
RETURN GET_TYPE_NUM("電撃")
;Elect

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_943
RETURN 1

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_943
RETURN 2

;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_943
RETURN 1

;　射程
@SKILL_RANGE_943,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_943,ARG
RETURN 3

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_943
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_943,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_943
RETURN GET_SUCCESSION_NUM("電撃")

;速度補正
@SKILL_SPEED_943, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_943,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_943
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_943
RETURN 10

@SKILL_RANKUP_943
RETURN 0

;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_943,ARG
RETURN GET_STATE_NUM("SHOCK")

;追加効果相性
@SKILL_ADDTIONAL_TYPE_943,ARG
RETURN GET_TYPE_NUM("電撃")

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_943,ARG
RETURN 15

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_943,ARG
RETURN 95


;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_943,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,943



;====================================================
;実行内容ここまで
;====================================================


;　 説明文の表示
@SKILL_EXPLAIN_943
PRINTL Lightning attack on all foes。Power is dependent on loyalty

;継承に必要な部位
@継承部位_943,ARG
RETURN 1

;使用条件
@SKILL_SPECIAL_ACTIONABLE_943,ARG
RETURN 1
;とくにない

