;通常攻撃
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_2921,ARG
RESULTS = 「Full Moon Howling」
;咆哮「満月の遠吠え」
SIF  GETBIT (FLAG:カスタムゲーム画面,1)
	RESULTS = 咆吼「満月の遠吠え」


;　命中
@SKILL_HITRATE_2921, ARG
RETURN 100

;　威力
@SKILL_POWER_2921, ARG
RETURN 0

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_2921,ARG
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_2921,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2921, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_2921,ARG
SIF FLAG:月齢 == 8
	RETURN 24
SIF FLAG:月齢 == 0
	RETURN 48
RETURN 36

;　相性
@SKILL_TYPE_2921,ARG
RETURN 0

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2921
RETURN 1
;魔法

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_2921
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_2921
RETURN 1

;　射程
@SKILL_RANGE_2921,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_2921,ARG
RETURN 3

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_2921
RETURN 4

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2921,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_2921
RETURN 5
;支援

;速度補正
@SKILL_SPEED_2921, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_2921,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_2921
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_2921
RETURN 7

@継承不能_2921
RETURN 1

;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_2921,ARG
RETURN 0

;追加効果相性
@SKILL_ADDTIONAL_TYPE_2921,ARG
RETURN 0

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_2921,ARG
RETURN 0

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_2921,ARG
RETURN 0

;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_2921,ARG,ARG:1
#LOCALSIZE 3
IF CFLAG:(ARG):PTフラグ == 0
	LOCAL:1 = 1
	LOCAL:2 = 7
ELSE
	LOCAL:1 = 7
	LOCAL:2 = 17
ENDIF

FOR LOCAL,LOCAL:1,LOCAL:2
	SIF POS(LOCAL) < 0
		CONTINUE
	CFLAG:POS(LOCAL):攻撃強化 = MAX(-16, CFLAG:POS(LOCAL):攻撃強化 - 8)
	CFLAG:POS(LOCAL):魔法威力強化 = MAX(-16, CFLAG:POS(LOCAL):魔法威力強化 - 8)
	CFLAG:POS(LOCAL):命中強化 = MIN(32, CFLAG:POS(LOCAL):命中強化 - 4)
	CFLAG:POS(LOCAL):回避強化 = MIN(32, CFLAG:POS(LOCAL):回避強化 - 4)
NEXT

PRINTFORMW \@ CFLAG:ARG:PTフラグ == 2 ? 敵 # 味方 \@の攻撃力を大幅に下げた！
PRINTFORMW \@ CFLAG:ARG:PTフラグ == 2 ? 敵 # 味方 \@の命中・回避力を下げた！






;====================================================
;実行内容ここまで
;====================================================

;====================================================
;戦闘中以外での実行
;====================================================


;====================================================
;実行内容ここまで
;====================================================


;　 説明文の表示
@SKILL_EXPLAIN_2921
PRINTL Releases a strange howl to intimidate enemies.
PRINTL Lowers the physical and magical attack power of all enemies by two levels and hit/avoidance by one level.
PRINTL MP cost increases on New Moon and decreases on Full Moon.
PRINTL Spell Card of Beast Imaizumi Kagerou.

;継承に必要な部位
@継承部位_2921,ARG
SIF TALENT:ARG:口
	RETURN 1
RETURN 0

;使用条件
@SKILL_SPECIAL_ACTIONABLE_2921,ARG
RETURN 1
;とくにない
