;通常攻撃
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_2929,ARG
RESULTS = 「Lunatic Gun」
;銃符「ルナティックガン」

;　命中
@SKILL_HITRATE_2929, ARG
RETURN 140

;　威力
@SKILL_POWER_2929, ARG
RETURN 50

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_2929,ARG
SIF ARG == -1
	RETURN 3
IF BASE:ARG:LV >= 49
	RETURN 10
ELSEIF BASE:ARG:LV >= 44
	RETURN 9
ELSEIF BASE:ARG:LV >= 39
	RETURN 8
ELSEIF BASE:ARG:LV >= 34
	RETURN 7
ELSEIF BASE:ARG:LV >= 29
	RETURN 6
ELSEIF BASE:ARG:LV >= 24
	RETURN 5
ELSEIF BASE:ARG:LV >= 19
	RETURN 4
ELSE
	RETURN 3
ENDIF


;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_2929,ARG
SIF ARG == -1
	RETURN 3
IF BASE:ARG:LV >= 49
	RETURN 10
ELSEIF BASE:ARG:LV >= 44
	RETURN 9
ELSEIF BASE:ARG:LV >= 39
	RETURN 8
ELSEIF BASE:ARG:LV >= 34
	RETURN 7
ELSEIF BASE:ARG:LV >= 29
	RETURN 6
ELSEIF BASE:ARG:LV >= 24
	RETURN 5
ELSEIF BASE:ARG:LV >= 19
	RETURN 4
ELSE
	RETURN 3
ENDIF

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2929, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_2929,ARG
RETURN 15

;　相性
@SKILL_TYPE_2929,ARG
RETURN GET_TYPE_NUM("飛具")
;飛具

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2929
RETURN 1
;どちらでもない

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_2929
RETURN 1

;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_2929
RETURN 1

;　射程
@SKILL_RANGE_2929,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_2929,ARG
RETURN 2

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_2929
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2929,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_2929
RETURN 23
;無し

;速度補正
@SKILL_SPEED_2929, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_2929,ARG


;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_2929
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_2929
RETURN 15

@SKILL_RANKUP_2929
RETURN 0

@継承不能_2929
RETURN 1
;不可

;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_2929,ARG
RETURN 0

;追加効果相性
@SKILL_ADDTIONAL_TYPE_2929,ARG
RETURN 0

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_2929,ARG
RETURN 0

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_2929,ARG
RETURN 0

;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_2929,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,2929






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_2929
;TODO PRINTL 敵一列からランダムに飛具属性で小威力の物理攻撃を3回行う
PRINTL Summons bullets from other dimensions to attack a row of enemy.
PRINTL Low-powered Gun attack, but number of hit increases as own level increases.
PRINTL Spell Card of Beast Seiran（専用）.

;継承に必要な部位
@継承部位_2929,ARG
RETURN 0

;使用条件
@SKILL_SPECIAL_ACTIONABLE_2929,ARG
RETURN 1
;とくにない
