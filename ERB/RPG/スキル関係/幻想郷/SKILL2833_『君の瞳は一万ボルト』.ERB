;通常攻撃
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_2833,ARG
RESULTS = 『Your Eyes Have The Sparkle Of 10,000 Volts』
;SIF  GETBIT (FLAG:カスタムゲーム画面,1)
;	RESULTS = 『君の瞳は一万ボルト』
;『君の瞳は一万ﾎﾞﾙﾄ』
;　命中
@SKILL_HITRATE_2833, ARG
RETURN 100

;　威力
@SKILL_POWER_2833, ARG
SIF ARG == -1
	RETURN 110
IF ABL:ARG:歌唱技能 == 0
	RETURN 110
ELSEIF ABL:ARG:歌唱技能 == 1
	RETURN 130
ELSEIF ABL:ARG:歌唱技能 == 2
	RETURN 150
ELSEIF ABL:ARG:歌唱技能 == 3
	RETURN 170
ELSEIF ABL:ARG:歌唱技能 == 4
	RETURN 190
ELSEIF ABL:ARG:歌唱技能 == 5
	RETURN 210
ELSEIF ABL:ARG:歌唱技能 == 6
	RETURN 240
ELSEIF ABL:ARG:歌唱技能 == 7
	RETURN 300
ELSEIF ABL:ARG:歌唱技能 == 8
	RETURN 350
ELSEIF ABL:ARG:歌唱技能 == 9
	RETURN 400
ELSE
	RETURN 400
ENDIF
RETURN 110

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_2833,ARG
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_2833,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2833, ARG
RETURN 3
;消費なし＝1

;　コストの量
@SKILL_COST_2833,ARG
SIF ARG == -1
	RETURN 9
IF ABL:ARG:歌唱技能 == 0
	RETURN 9
ELSEIF ABL:ARG:歌唱技能 == 1
	RETURN 12
ELSEIF ABL:ARG:歌唱技能 == 2
	RETURN 15
ELSEIF ABL:ARG:歌唱技能 == 3
	RETURN 18
ELSEIF ABL:ARG:歌唱技能 == 4
	RETURN 23
ELSEIF ABL:ARG:歌唱技能 == 5
	RETURN 26
ELSEIF ABL:ARG:歌唱技能 == 6
	RETURN 29
ELSEIF ABL:ARG:歌唱技能 == 7
	RETURN 34
ELSEIF ABL:ARG:歌唱技能 == 8
	RETURN 38
ELSEIF ABL:ARG:歌唱技能 == 9
	RETURN 41
ELSE
	RETURN 45
ENDIF
RETURN 9

;　相性
@SKILL_TYPE_2833,ARG
RETURN 6
;Elect

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2833
RETURN 1
;EXTRA

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_2833
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_2833
RETURN 1

;　射程
@SKILL_RANGE_2833,ARG
RETURN 3

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_2833,ARG
RETURN 2

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_2833
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2833,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_2833
RETURN 9
;Elect

;速度補正
@SKILL_SPEED_2833, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_2833,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_2833
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_2833
RETURN 12


;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_2833,ARG
RETURN GET_STATE_NUM("SHOCK")

;追加効果相性
@SKILL_ADDTIONAL_TYPE_2833,ARG
RETURN 6

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_2833,ARG
RETURN 20

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_2833,ARG
RETURN 95
;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_2833,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,2833





;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_2833
PRINTL 敵一列にシビレる稲妻のような歌を聞かせ、ダメージを与える。
PRINTL 使用者の歌唱技能によって威力が増加する。
PRINTL 遥か連ダコ惑星から幻想郷に伝わった、『歌武器』と呼ばれる異能の力。

;継承に必要な部位
@継承部位_2833,ARG
SIF TALENT:ARG:口
	RETURN 1
RETURN 0

;使用条件
@SKILL_SPECIAL_ACTIONABLE_2833,ARG
RETURN 1
;とくにない

