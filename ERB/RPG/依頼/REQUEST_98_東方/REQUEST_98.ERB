

@PLAYABLE_REQUEST_98
SIF GETBIT(FLAG:EXTRAイベント設定, 0) == 0
	RETURN 0
SIF 依頼フラグ:98:0 != 0 
	RETURN 0
IF FLAG:東方依頼進行度 == 0
	RESULTS = (東方MOD)新種悪魔の討伐
	RETURN 1
ELSEIF FLAG:東方依頼進行度 == 1
	RESULTS = (東方MOD)謎の悪霊
	RETURN 1
ELSEIF FLAG:東方依頼進行度 == 2
	RESULTS = (東方MOD)秘神の求人？
	RETURN 1
ENDIF

@EXPLAIN_REQUEST_98
IF FLAG:東方依頼進行度 == 0
	CALL EXPLAIN_REQUEST_98_01
ELSEIF FLAG:東方依頼進行度 == 1
	CALL EXPLAIN_REQUEST_98_02
ELSEIF FLAG:東方依頼進行度 == 2
	CALL EXPLAIN_REQUEST_98_03
ENDIF

@REQUEST_98
IF FLAG:東方依頼進行度 == 0
	CALL REQUEST_98_01
ELSEIF FLAG:東方依頼進行度 == 1
	CALL REQUEST_98_02
ELSEIF FLAG:東方依頼進行度 == 2
	CALL REQUEST_98_03
ENDIF

