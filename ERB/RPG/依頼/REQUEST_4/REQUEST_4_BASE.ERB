;====================
;関連フラグリスト
;====================
;====================
;条件
;====================
@PLAYABLE_REQUEST_4
LOCAL = STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]])
LOCAL:1 = STRFLAG_NUM_EV("罰ルート進行", , , , [[イベント:ペルソナ２]])
IF LOCAL == 0 && LOCAL:1 == 0
	IF [[依頼フラグ:軽子坂高校体育館(1)]] && [[依頼フラグ:軽子坂高校体育館(2)]]
		;依頼:ペルソナ２
		RESULTS = Seven Sisters High School Investigation Request
		RETURN 1
	ENDIF
ENDIF
IF LOCAL == 5
	IF STRFLAG_NUM_EV("ヴィジュアル系番長の噂", , , , [[イベント:噂]]) == 2
		RESULTS = Gas Chamber Live
	ELSE
		RESULTS = ＭＵＳＥＳ Concert
	ENDIF
	RETURN 1
ENDIF
IF LOCAL:1 == 7 && STRFLAG_NUM_REQ("P6クリア日", , , , [[依頼:ペルソナ２]]) + 2 < DAY
	RESULTS = New ＪＯＫＥＲ
	RETURN 1
ENDIF
IF LOCAL:1 == 8 && STRFLAG_NUM_REQ("P7クリア日", , , , [[依頼:ペルソナ２]]) + 2 < DAY
	RESULTS = The New World Order
	RETURN 1
ENDIF
;#;RESULTS = ペルソナ２依頼
;#;RETURN 1
RETURN 0
;====================
;説明
;====================
@EXPLAIN_REQUEST_4
LOCAL = STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]])
LOCAL:1 = STRFLAG_NUM_EV("罰ルート進行", , , , [[イベント:ペルソナ２]])
IF LOCAL == 0 && LOCAL:1 == 0
	SIF [[依頼フラグ:軽子坂高校体育館(2)]] && [[依頼フラグ:軽子坂高校体育館(2)]]
		CALL EXPLAIN_REQUEST_4_GP0
ENDIF
IF LOCAL == 5
	IF STRFLAG_NUM_EV("ヴィジュアル系番長の噂", , , , [[イベント:噂]]) == 2
		CALL EXPLAIN_REQUEST_4_G5B
	ELSE
		CALL EXPLAIN_REQUEST_4_G5A
	ENDIF
ENDIF
;====================
;内容
;====================
@REQUEST_4
LOCAL = STRFLAG_NUM_EV("罪ルート進行", , , , [[イベント:ペルソナ２]])
LOCAL:1 = STRFLAG_NUM_EV("罰ルート進行", , , , [[イベント:ペルソナ２]])
CLEARBIT FLAG:依頼出現1, 4
IF LOCAL == 0 && LOCAL:1 == 0
	SIF [[依頼フラグ:軽子坂高校体育館(2)]] && [[依頼フラグ:軽子坂高校体育館(2)]]
		CALL REQUEST_4_GP0
ENDIF
IF LOCAL == 5
	IF STRFLAG_NUM_EV("ヴィジュアル系番長の噂", , , , [[イベント:噂]]) == 2
		CALL REQUEST_4_G5B
	ELSE
		CALL REQUEST_4_G5A
	ENDIF
ENDIF
IF LOCAL:1 == 7 && STRFLAG_NUM_REQ("ルート進行日", , , , [[イベント:ペルソナ２]]) + 2 < DAY
	CALL REQUEST_4_P7
	CALLF STRFLAG_NUM_EV("罰ルート進行", "=", , 8, [[イベント:ペルソナ２]])
ENDIF

IF LOCAL:1 == 8 && STRFLAG_NUM_REQ("ルート進行日", , , , [[イベント:ペルソナ２]]) + 2 < DAY
	CALL REQUEST_4_P8
	CALLF STRFLAG_NUM_EV("罰ルート進行", "=", , 9, [[イベント:ペルソナ２]])
ENDIF
CALLF STRFLAG_NUM_EV("ルート進行日", "=", , DAY, [[イベント:ペルソナ２]])
;#;PRINTL 入力
;#;INPUTS
;#;TRYCALLFORM REQUEST_4_%RESULTS%
;#;RETURN 0
