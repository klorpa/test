;-------------------------------------------------
;汚れ判定関数
;ARGのARGSについている汚れを判定する
;愛液と母乳と元々その部位についていた汚れは対象外
;汚れの個数と、汚れの合計(つまり、愛液と母乳を除いた汚れ情報を返す)
;-------------------------------------------------
@DIRTY, ARGS, ARG = -99
#FUNCTION
RESULT = 0
RESULT:1 = 0
SIF ARG == -99
	ARG = TARGET
;IF ARGS != ヴァギナ" && GET_STAIN(ARGS, "愛液", ARG)
;	RESULT++
;	RESULT:1 |= 1
;ENDIF
IF ARGS != "ペニス" && GET_STAIN(ARGS, "ペニス", ARG)
	RESULT++
	RESULT:1 |= 2
ENDIF
IF GET_STAIN(ARGS, "精液", ARG)
	RESULT++
	RESULT:1 |= 4
ENDIF
IF ARGS != "アナル" && GET_STAIN(ARGS, "アナル", ARG)
	RESULT++
	RESULT:1 |= 8
ENDIF
;IF GET_STAIN(ARGS, "母乳", ARG)
;	RESULT++ 
;	RESULT:1 |= 16
;ENDIF
IF GET_STAIN(ARGS, "粘液", ARG)
	RESULT++
	RESULT:1 |= 32
ENDIF
IF GET_STAIN(ARGS, "破瓜の血", ARG)
	RESULT++
	RESULT:1 |= 64
ENDIF
RETURNF RESULT 