;===============================
;合体可能な仲魔（悪魔）の人数
;==============================
@NUM_FUSIONABLE()
#FUNCTION
LOCAL:1 = 0
FOR LOCAL,0,CHARANUM
	SIF ABL:LOCAL:種族 < 1 || ABL:LOCAL:種族 > 44
		CONTINUE
	SIF CFLAG:LOCAL:合体不可
		CONTINUE
	SIF CFLAG:LOCAL:この場に居ないフラグ
		CONTINUE
	SIF CFLAG:LOCAL:PTフラグ == 0
		CONTINUE
	LOCAL:1 += 1
NEXT
RETURNF LOCAL:1