;===============================
;仲魔（悪魔）の人数
;==============================
@NUM_NAKAMA()
#FUNCTION
LOCAL:1 = 0
FOR LOCAL,0,CHARANUM
	SIF ABL:LOCAL:種族 < 1 || ABL:LOCAL:種族 > 45
		CONTINUE
	SIF CFLAG:LOCAL:容量未使用
		CONTINUE
	SIF CFLAG:LOCAL:所属ＣＯＭＰ == -1
		CONTINUE
	SIF CFLAG:LOCAL:PTフラグ == 0
		CONTINUE
	LOCAL:1 += 1
NEXT

RETURNF LOCAL:1