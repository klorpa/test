;-------------------------------------------------
;文字インプット関数@INPUT_CHAR
;引数ARGSに含まれる文字が入力されるまで文字の入力を待ちます
;有効な文字が入力されたらそれをRESULTSへ格納します
;全角文字には対応していません
;-------------------------------------------------
;ARG 値がゼロでない場合、リターンキー("")を有効な入力とみなします。
@INPUT_CHAR(ARGS,ARG = 0)
#LOCALSIZE 1
ONEINPUTS
SIF ARG != 0 && RESULTS == ""
	RETURN
FOR LOCAL,0,STRLENS(ARGS),1
	SIF RESULTS == SUBSTRING(ARGS,LOCAL,1)
		RETURN
NEXT
SIF RESULTS == ""
	RESTART
CLEARLINE 1
REUSELASTLINE 
RESTART