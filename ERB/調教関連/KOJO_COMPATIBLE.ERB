;追加要素とかに対して口上で互換性をもたせられたら嬉しい関数
;主に EVENT_K.ERB @口上呼び出し での使用を想定

;====================================
;　口上呼び出し前処理
;　呼び出したいCOM番号に差し替える
;　引数1　: ARG   = 対象キャラ(東方系とか判定して互換したり
;
;　戻り値 : 差し替え前のCOM番号
;====================================
@KOJO_COMPATIBLE_BOFORE, ARG
	;逆レイプ(アナル)の場合
	IF SELECTCOM == 75
		SELECTCOM = 66
		LOCAL     = 75
	ENDIF

	RETURN LOCAL

;====================================
;口上呼び出し後処理
;口上以外の処理呼び出しため差し替えた番号を元に戻す
;　引数1　: ARG   = 口上キャラのNo
;　引数2　: ARG:1 = 差し替え対象のCOM番号
;
;　戻り値 : 特に意味なし
;====================================
@KOJO_COMPATIBLE_AFTER, ARG, ARG:1

	SELECTCOM = ARG:1

	RETURN 1
