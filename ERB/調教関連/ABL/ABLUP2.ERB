;技巧の可否
@ISABLUP2
;恋慕、親愛、服従、隷属はMAXLV10
IF ABL:2 >= 10
	RETURN 0
ELSEIF ABL:2 >= 5 && !陥落(TARGET) && TARGET != MASTER
	RETURN 0
ENDIF

CALL DECIDE_ABLUP2

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF


;Technique
@ABLUP2
$ABLUP_LOOP

DRAWLINE
;恋慕、親愛、服従、隷属はMAXLV10
IF ABL:2 >= 10
	PRINTW The abiltiy has been MAXed already.
	RETURN 0
ELSEIF ABL:2 >= 5 && !陥落(TARGET) && TARGET != MASTER
	PRINTW The abiltiy has been MAXed already.
	RETURN 0
ENDIF

CALL DECIDE_ABLUP2

PRINT [0] - 
PRINTS PALAMNAME_E(8)
PRINT  Orbs×
PRINTV A
PRINT ....
IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT Not Enough Orbs 
	SIF I & 2
		PRINT Not Enough EXP 
ENDIF
PRINTL 

IF Z == 0
	WAIT
	RETURN
ENDIF

PRINTLC [100] - Quit
PRINTFORMLC   %" " * 42%
PRINTLC [200] - Auto-Upgrade ability


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100 && RESULT != 200
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL Conditions are not met
	RETURN 0
ELSEIF RESULT == 100
	RETURN 0
ELSEIF RESULT == 200
	CALL AUTOMAX, 2
	RETURN 0
ENDIF

ABL:2 += 1

IF RESULT == 0
	JUEL:8 -= A
ENDIF

PRINTS ABLNAME_E(2)
PRINT  ability level became 
PRINTV ABL:2
PRINTW .
GOTO ABLUP_LOOP

;技巧LVUP判定
@DECIDE_ABLUP2
;条件別にＯＫかダメかを記録する
I = 0
;能力アップ可能なら1にする
Z = 0

IF ABL:2 == 0
	A = 1
ELSEIF ABL:2 == 1
	A = 20
ELSEIF ABL:2 == 2
	A = 200
ELSEIF ABL:2 == 3
	A = 3000
ELSEIF ABL:2 == 4
	A = 20000
ELSEIF ABL:2 == 5
	A = 25000
ELSEIF ABL:2 == 6
	A = 34000
ELSEIF ABL:2 == 7
	A = 45000
ELSEIF ABL:2 == 8
	A = 55000
ELSEIF ABL:2 == 9
	A = 70000
ENDIF

;自尊心
IF TALENT:15
	TIMES A , 1.50
ENDIF
;一線越えない
IF TALENT:27 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && 陥落() < 2)
	TIMES A , 2.00
ENDIF

;0になった場合は1にする
SIF A == 0
	A = 1

;習得の珠で上げる
SIF JUEL:8 < A
	I |= 1

IF I == 0
	Z = 1
ENDIF
RETURN 1


