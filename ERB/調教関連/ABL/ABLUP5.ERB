;Ç`ä¥äoÇÃâ¬î€
@ISABLUP5
;óˆïÁÅAêeà§ÅAà˙óêÅAè©ïwÇÕMAXLV10
IF ABL:5 >= 10
	RETURN 0
ELSEIF ABL:5 >= 5 && !ä◊óé(TARGET) && TARGET != MASTER
	RETURN 0
ENDIF

CALL DECIDE_ABLUP5

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF


;Ç`Sensitivity
@ABLUP5
$ABLUP_LOOP

DRAWLINE
;óˆïÁÅAêeà§ÅAà˙óêÅAè©ïwÇÕMAXLV10
IF ABL:5 >= 10
	PRINTW The abiltiy has been MAXed already.
	RETURN 0
ELSEIF ABL:5 >= 5 && !ä◊óé(TARGET) && TARGET != MASTER
	PRINTW The abiltiy has been MAXed already.
	RETURN 0
ENDIF

CALL DECIDE_ABLUP5

PRINT [0] - 
PRINTS PALAMNAME_E(2)
PRINT  OrbsÅ~
PRINTV A
PRINT , 
PRINTS EXPNAME_E(1)
PRINTV B
PRINT  required
PRINT ....
IF I == 0
	PRINT ÇnÇj
ELSE
	SIF I & 1
		PRINT Not Enough Orbs 
	SIF I & 2
		PRINT Not Enough EXP 
ENDIF
PRINTL 

IF Z == 0
	WAIT
	RETURN
ENDIF

PRINTLC [100] - Quit
PRINTFORMLC   %" " * 42%
PRINTLC [200] - Auto-Upgrade ability


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100 && RESULT != 200
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL Conditions are not met
	RETURN 0
ELSEIF RESULT == 100
	RETURN 0
ELSEIF RESULT == 200
	CALL AUTOMAX, 5
	RETURN 0
ENDIF

ABL:5 += 1

IF RESULT == 0
	JUEL:2 -= A
ENDIF

PRINTS ABLNAME_E(5)
PRINT  ability level became 
PRINTV ABL:5
PRINTW .
GOTO ABLUP_LOOP


;Ç`ä¥äoLvUPîªíË
@DECIDE_ABLUP5
;èåèï Ç…ÇnÇjÇ©É_ÉÅÇ©ÇãLò^Ç∑ÇÈ
I = 0
;î\óÕÉAÉbÉvâ¬î\Ç»ÇÁ1Ç…Ç∑ÇÈ
Z = 0

IF ABL:5 == 0
	A = 1
	B = 2
ELSEIF ABL:5 == 1
	A = 50
	B = 10
	SIF EXP:1 >= EXPLV:3
		A = 20
ELSEIF ABL:5 == 2
	A = 400
	B = 30
	SIF EXP:1 >= EXPLV:4
		A = 100
ELSEIF ABL:5 == 3
	A = 5000
	B = 75
	SIF EXP:1 >= EXPLV:5
		A = 4000
ELSEIF ABL:5 == 4
	A = 15000
	B = 150
	SIF EXP:1 >= EXPLV:5
		A = 8000
ELSEIF ABL:5 == 5
	A = 30000
	B = 230
	SIF EXP:1 >= EXPLV:5
		A = 24000
ELSEIF ABL:5 == 6
	A = 40000
	B = 270
ELSEIF ABL:5 == 7
	A = 50000
	B = 360
ELSEIF ABL:5 == 8
	A = 60000
	B = 420
ELSEIF ABL:5 == 9
	A = 70000
	B = 500
ENDIF

;à˙óê
IF TALENT:4
	TIMES A , 0.80
	TIMES B , 0.80
ENDIF
;è©ïw
IF TALENT:7
	TIMES A , 0.50
	TIMES B , 0.50
ENDIF

;0Ç…Ç»Ç¡ÇΩèÍçáÇÕ1Ç…Ç∑ÇÈ
SIF A == 0
	A = 1

;âıÇ`ÇÃéÏÇ≈è„Ç∞ÇÈ
SIF JUEL:2 < A
	I |= 1

SIF EXP:1 < B
	I |= 2

IF I == 0
	Z = 1
ENDIF

RETURN 1

