;éBâeãZî\ÇÃâ¬î€
@ISABLUP13
;óˆïÁÅAêeà§ÅAïûè]ÅAóÍëÆÇÕMAXLV10
IF ABL:13 >= 10
	RETURN 0
ELSEIF ABL:13 >= 5 && !ä◊óé(TARGET) && TARGET != MASTER
	RETURN 0
ENDIF

CALL DECIDE_ABLUP13

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF

;Photography
@ABLUP13
$ABLUP_LOOP

DRAWLINE
;óˆïÁÅAêeà§ÅAïûè]ÅAóÍëÆÇÕMAXLV10
IF ABL:13 >= 10
	PRINTW The abiltiy has been MAXed already.
	RETURN 0
ELSEIF ABL:13 >= 5 && !ä◊óé(TARGET) && TARGET != MASTER
	PRINTW The abiltiy has been MAXed already.
	RETURN 0
ENDIF

CALL DECIDE_ABLUP13

PRINT [0] - 
PRINTS PALAMNAME_E(8)
PRINT  OrbsÅ~
PRINTV A
PRINT , 
PRINTS EXPNAME_E(62)
PRINTV B
PRINT  required
PRINT ....
IF I == 0
	PRINT ÇnÇj
ELSE
	SIF I & 1
		PRINT Not Enough Orbs 
	SIF I & 2
		PRINT Not Enough EXP 
ENDIF
PRINTL 

IF Z == 0
	WAIT
	RETURN
ENDIF

PRINTLC [100] - Quit
PRINTFORMLC   %" " * 42%
PRINTLC [200] - Auto-Upgrade ability


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100 && RESULT != 200
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL Conditions are not met
	RETURN 0
ELSEIF RESULT == 100
	RETURN 0
ELSEIF RESULT == 200
	CALL AUTOMAX, 13
	RETURN 0
ENDIF

ABL:13 += 1

IF RESULT == 0
	JUEL:8 -= A
ENDIF

PRINTS ABLNAME_E(13)
PRINT  ability level became 
PRINTV ABL:13
PRINTW .
GOTO ABLUP_LOOP


;éBâeãZî\LVUPîªíË
@DECIDE_ABLUP13
;èåèï Ç…ÇnÇjÇ©É_ÉÅÇ©ÇãLò^Ç∑ÇÈ
I = 0
;î\óÕÉAÉbÉvâ¬î\Ç»ÇÁ1Ç…Ç∑ÇÈ
Z = 0

IF ABL:13 == 0
	A = 75
	B = 5
ELSEIF ABL:13 == 1
	A = 500
	B = 10
ELSEIF ABL:13 == 2
	A = 3000
	B = 30
ELSEIF ABL:13 == 3
	A = 9000
	B = 50
ELSEIF ABL:13 == 4
	A = 24000
	B = 100
ELSEIF ABL:13 == 5
	A = 30000
	B = 200
ELSEIF ABL:13 == 6
	A = 35000
	B = 300
ELSEIF ABL:13 == 7
	A = 40000
	B = 400
ELSEIF ABL:13 == 8
	A = 45000
	B = 500
ELSEIF ABL:13 == 9
	A = 50000
	B = 600
ENDIF

;óˆïÁ
IF TALENT:3
	TIMES A , 0.60
	TIMES B , 0.60
ENDIF
;êeà§
IF TALENT:6
	TIMES A , 0.40
	TIMES B , 0.40
ENDIF
;àÍê¸âzÇ¶Ç»Ç¢
IF TALENT:27 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && ä◊óé() < 2)
	TIMES A , 2.00
	TIMES B , 2.00
ENDIF

;èKìæÇÃéÏÇ≈è„Ç∞ÇÈ
SIF JUEL:8 < A
	I |= 1

SIF EXP:62 < B
	I |= 2

IF I == 0
	Z = 1
ENDIF

RETURN 1


