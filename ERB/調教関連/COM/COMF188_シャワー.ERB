;-------------------------------------------------
;シャワー
;特殊コマンド
;-------------------------------------------------
@ACT_COM188
PRINTL Shower
TSTR:0 = シャワー
;CALL KOJO_MESSAGE_COM

;着脱の処理　装備時はソース、経験の加算は行わない
IF TEQUIP:32 == 1
	TEQUIP:32 = 0
ELSE
	TEQUIP:32 = 1
ENDIF

;依存度ベクトル
TFLAG:33 = 0


RETURN 1


;-------------------------------------------------
;装着継続の処理
;-------------------------------------------------
@EQUIP_COM188
;シャワー使用中

PRINTL ＜シャワー使用中＞

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、Willpower
DOWNBASE:TARGET:体力 += 0
DOWNBASE:TARGET:気力 += 10

SOURCE:TARGET:恭順 += 100
SOURCE:TARGET:鬱屈 += 100
SOURCE:TARGET:反感 += 50

;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;吸血鬼（流水の中では動けないため、いろいろ追加）
IF TALENT:132
	SOURCE:TARGET:恐れ += 200
	SOURCE:TARGET:屈従 += 200
	SOURCE:TARGET:不潔 += 500
	SOURCE:TARGET:逸脱 += 500
	SOURCE:TARGET:反感 += 500
ENDIF

;動物耳の場合（まぁ基本洗われるのは嫌と解釈）
IF TALENT:143
	SOURCE:TARGET:恐れ += 100
	SOURCE:TARGET:屈従 += 100
	SOURCE:TARGET:逸脱 += 500
	SOURCE:TARGET:反感 += 500
ENDIF

;猫科の場合(超びびる。愛があれば無効)
IF (NO == [[キャラ:ネコマタ]] || NO == [[キャラ:バステト]]) && Affection() == 0 && Love() == 0
	SOURCE:TARGET:恐れ += 400
	SOURCE:TARGET:屈従 += 200
	SOURCE:TARGET:逸脱 += 250
	SOURCE:TARGET:反感 += 500
	DOWNBASE:TARGET:体力 += 30
	DOWNBASE:TARGET:気力 += 50
ENDIF
;-------------------------------------------------
;汚れの処理
;-------------------------------------------------
;シャワーで汚れは常にリセット
RESET_STAIN TARGET

;潤滑を1000減らす
IF PALAM:4 > 1000
	PALAM:4 -= 1000
ELSE
	PALAM:4 = 0
ENDIF

RETURN 1

;===================================================
;調教タイプ
;===================================================
@COMTYPE_188
RETURN COMTYPE("特殊コマンド系")


