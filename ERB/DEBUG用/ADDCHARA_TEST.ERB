

@SHOPCOMABLE_192
[IF_DEBUG]
	RESULTS = Add Slave
	RETURN 1
[ENDIF]
IF FLAG:DEBUG
	RESULTS = Add Slave
	RETURN 1
ENDIF
RETURN -1

@SHOP_COM_192
PRINTL 奴隷にするキャラ番号を入力してください

INPUT

IF  EXISTCSV(RESULT, 0)
	CALL ADD_NEW_COMPANION,RESULT,0,0
	;デバッグ用に戦闘参加出来るように変更
	CFLAG:RESULT:戦闘参加不可能 = 0
ELSE
	PRINTL その番号のキャラは存在しません
ENDIF

